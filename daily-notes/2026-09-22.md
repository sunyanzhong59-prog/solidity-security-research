# 2026-09-22（Day 69）

## 今日状态

### 🎯 主战线：空档第 31 天（落笔时三源未出，基线取 9/21 全空）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：今日 07:30 金标流水线 / 09:00 三源侦察档产物（`scope-diff-2026-09-22.md` / `competitions-recon-2026-09-22.md`）均未生成，待跑完后核对。
- **9/21 侦察基线（一手）**：Immunefi **新增 0 / 下架 0 / maxBounty 变更 0**；72h 新资产 53 条**全为既有 program 资产扩容**（Aera 多链 TransferBlacklistHook / Whitelist / MultiDepositorVault / Provisioner / PriceAndFeeCalculator，gtUSDa·gtBTC·bgBTC 系列）→ 非新 bounty、不产生新竞争窗口。
  - **Sherlock**：唯一 ongoing = Ethereum Fusaka Upgrade（$2M，9/15–10/13）核心协议升级**非 DeFi → 排除**；其余 301 场全 FINISHED。
  - **CodeHawks**：全部竞赛已结束（最新 BattleChain 2026-07），无 live/upcoming。
  - **Cantina**：`cantina_diff_20260921.json` liveCount=51，与 9/19 建立的 51 条基线逐 UUID 比对 `NEW=[]` / `GONE=[]` → **0 新赛、0 下架**。
- **空档纪律（铁律⑪）**：练手仅在用户明确说「练」时开；否则静默监控金标，想干真活就深审 parked / 新筛的真靶。

### 🔴 今日主线 1：早班探针（05:43 用户驱动，手动补早班）= 无鲜靶
- `gold_pipeline.py --dry`（前台，EXIT=0）：**gold=0 / killed=0 / clean_candidates=0 / intel=0**（Sherlock / CodeHawks）。→ **Immunefi GOLD=0 连续第 53 天**（8/31 → 9/22，稳定空窗）。
- `killed=0` 仍为**源覆盖不全异常**（薄子集）→「绝对 0 候选」未 100% 坐实，需记住这是 scanner 局限而非市场事实。
- **leads=5（↑1 vs 昨日 4）**：COLD×4 = `battlechain-confidence-pools`（CodeHawks 历史赛，47d 前 finalised）/ `idle-labs/idle-tranches`（55★，0d 前 push）/ `flayerlabs/flaunchgg-contracts`（68★，0d 前 push）/ `invasivecape/ghost-protocol`（3★，0d 前 push）；WARM×1 = `morpho-org/vault-v2`（104★，知名协议，竞争高）。
- **`leads_matches=0`** → 5 条全未匹配活跃 Immunefi / Sherlock 池，**非 v2 甜区靶**（仅 GitHub-fresh 仓库，尚待核实有无对应在跑赏金）。→ 维持「空档静默监控金标」。
- **用户提问「自动检测不出来吗」的解答（认知修正）**：① 自动化本身不主动 ping 用户（**无 push 通道**：QQ邮箱 / Discord 连接器均 disconnected），结果静默写日志 → 表面像"没检测"；② scanner 源薄（`killed=0`）覆盖不全，可能漏靶；③ 早班原先依赖 18:00 晚班，延迟 ~18h（见主线 3 已修复为早晚双档）。

### 🔴 今日主线 2：自动化注册表核查 + 坏路径修复
- **误述纠正**：前一轮称"只有 18:00 一次/一天"，实际**早班 gold_pipeline 早已存在**（`automation-1788146085830`，每天 07:30，cwds 正确可工作）——当时仅锚了日志晚 6 点档、未查注册表，属铁律①「先查证再下结论」的自犯，已纠正。
- **真缺口定位**：18:00 晚班（`automation-1788914189938`）**cwds 指向 `D:\c\Users\...`（多了一个 `c\`，路径不存在）** → 晚班很可能跑偏/失败。
- **用户决策**：**保持 07:30 不动**（不加 06:30 早班，避免与 07:30 重复浪费 + 触发 Immunefi 源限流）。
- **动作**：修复 `1788914189938` 的 cwds → `C:\Users\Administrator\WorkBuddy\2026-06-26-19-10-30`（与 07:30 一致），update 成功、nextRunAt 已排程 → **早晚双档（07:30 工作 + 18:00 复活）真实跑通**。

### 🔴 今日主线 3：自动化清理（删僵尸任务 + 双显 bug 定性）
- **删 #13**：用户质疑「Hats 侦察一次性」为何是僵尸 → 查实 `automation-1788814766182` 为 `once`、scheduledAt=2026-09-08T05:05（**已过期 14 天**），一次性任务最多触发一次 → 未来绝不再触发 = 死重。**Hats 侦察功能未丢**（已并入 `1788914189938` 晚 6 点「五大情报源含 Hats」）→ 用户授权后 delete 成功。
- **「晚 6 点双显」定性 = 列表渲染 bug，非真重复**：`1788914189938` view 返回单条健康记录（现名五大情报源 + cwds 正确）；list 把它 Echo 两行（现名 + 旧缓存名）→ **DB 仅一行，无需合并/删除**。

### 🔴 今日主线 4：按用户开机窗口（05:30–18:20）重排全部自动化时间
- 根因：用户指出 00:00×2 与 21:00 落在其关机时段 = **从不触发**。核查 + 重排：
  - `automation-1789611690686` 免费平台新鲜靶监视：00:00 → **06:00**
  - `automation-1785021095983` 每天完整备份到 D 盘：00:00 → **17:45**（end-of-day 备份）
  - `automation-1788400681358` Canopy streak 提醒：21:00 → **17:00**
  - `automation-1788994312380` Mezo #346 跟踪：原 rrule 无 BYHOUR（默认 00:00）→ **12:00 每 2 天**
  - `automation-1788914189938` 晚 6 点侦察：18:00 → **17:30**（距关机留 50min 缓冲）
- **结果**：全 12 条自动化现已全部落在 05:30–18:20 窗口内。
- **⚠️ 根因提醒（必须记住）**：**自动化仅在 WorkBuddy 开着时才跑**；需保持 app 在 05:30–18:20 全程运行，否则该时刻任务被跳过（不一定补跑）。→ 这是"空档期日志缺失"的隐藏原因之一。
- **副线**：`testnet-recon`(08:00) prompt 写"不写文件"→ 结果静默不可见（同无 push 通道老问题），建议改 prompt 落日志（待用户授权）；**Canopy streak 保留确认**（用户拍板，testnet 副线，成本极低 + 空投期权；设三道退出闸 = 不发币 / 不映射主网 / 项目关停即弃；副线严守 20min 上限防演化成黑洞）。
- **落盘**：`bounty-recon/自动化与铁律总表_v2.md` / `_v3.md`（12 条自动化最新排程 + 16 条铁律 + 变更附注，打印用）。

### 📌 真靶战线现状（承接 9/21）
- **⭐ twyne #55649（Immunefi，Critical）→ 今日为关键节点**：Escalated @ 9/20 16:08 → **48h 项目确认锚 = 今日 ~16:08**，336h 解决锚 ~10/4 16:08。**今日 16:08 后若无确认动静 → 用 "ask for help" 让 Immunefi 重新介入（唯一该动的按钮）**；等待期纪律：不重复提交同根因、不催评论区。
- **TermMax V2（Immunefi）→ PRIMARY 候选**：⑬ 同根因查重 PASS（program 页 info/scope 中 bad debt / share price / redeem / dealBadDebt / ERC4626 / accounting 出现 0 次）+ ⑫ hunter 19 paid 可接受；报告九段式草稿已落盘（`bounty-recon/audit-notes/termmax-immunefi-report.md`，状态 LIVE / ready to submit）。**待办**：soldeer 依赖装完后编译真实 `TermMaxVaultV2` + `OrderManagerV2` 跑 mainnet-fork 集成 PoC + diff 部署字节码（屏蔽 immutable）→ 无撞车则 Critical 主张走用户 Edge 提交（NoKYC）。
- **Midas #122 → 已结案**：#355 + #356 双 Duplicate，$40 押金全退、零亏损零奖金。**不再对该 program 投资本**。
- **TENOR-001（Sherlock #407）→ parked**：严重度只有 批判/高 两档且全需 250 USDC 存款，**无 Medium 档**；Tenor 官方安全页明示 bounty 只跑 Sherlock、无免费镜像 → 技术上仍有效但明确不值得投资。
- **Hinkal（HackenProof）→ 不提交**：四透镜深审无提交级候选 → EV=0，按铁律⑩不提交（省 amlbot KYC 触发与精力）。
- **orderly `contract-evm` / gammaswap v1-core → 各经四透镜 CLEAN，不提交**（资金面全 facade 门控 / EIP-712 + withdrawNonce 重放锁死 / dispatcher 无 lock 但 leaf 全带 lock → 回调重入一律 revert `Locked()`）。审计笔记 `bounty-recon/audit-notes/orderly-2026-09-21.md`、`bounty-recon/gammaswap/audit-notes/gammaswap-core-2026-09-20.md`。
- **✗ 不为「有靶打」放宽过滤器**：Alchemix v3（EVM + 免费 + $150K，但上线 ≈204 天超 90 天红线）→ 不放宽。

### 🧪 练手资产（空档期沉淀，零 bounty EV、一律不提交）
- **盲卡库 38 张**：`total=38 / 冷审通过=38（全 4/4 PASS）/ Critical=15 / High=22 / Medium=1 / 覆盖族=38 / 结构化答案键=32`。面板 `bounty-recon/live-audit/blind-coverage-matrix.html`。
- **脏代码 spotting 训练样本**：`practice/alejo-vault/`（5 发现 + Foundry PoC 5/5 PASS + 五份合规报告练习，校准出「5 疑似里 1 真 bug」的双向偏置）/ `practice/nico-lending/`（无 High/Critical 可提交漏洞）/ `practice/blaze-phoenix/`（代码质量极高、零 Critical，不提交）。
- **其余靶**：`practice/siyam-perp/`（Critical 资金锁 4/4 PASS）/ `practice/maxwire-staking/`（诚实证伪对照 5/5 PASS）/ `practice/hinkal-revisit/` / `practice/starknet-l1-solidity/` / DeFi Saver V3（R1–R8 全 FALSIFIED，44 tests PASS）。
- **待令靶**：YieldNest Max Yield Vault（`practice/yieldnest-vault/`，audit-notes 骨架已写）。**未获「练」指令前不动，严禁提交任何 finding（铁律⑩）。**

### 🛠 工具 / 流程改进待办
- `cantina_diff.js` 的 ended 判定逻辑重写（按 card 内独立区块 / `href` 解析，替代整页关键词匹配）—— 连续两日误报，须修；**结论一律以 UUID 集合比对为准**。
- TermMax mainnet-fork PoC：soldeer 依赖安装完成后编译真实合约并与部署字节码 diff（屏蔽 immutable），证明「本地源码 == 主网代码」。
- 自动化结果可见性：无 push 通道 + 部分 prompt 不落文件 → 结果静默；评估统一改为「落日志 + 用户开 app 时自读」或后续接通知通道。
