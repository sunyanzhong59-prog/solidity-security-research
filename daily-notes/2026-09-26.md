# 2026-09-26（Day 73）

## 今日状态

### 🎯 主战线：空档第 35 天（系列计数；金标连续空窗自 8/31 = 26 天）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：今日 07:30 金标流水线 / 09:00 三源侦察档产物**均未生成**，待跑完后核对。今日结论**不依赖**它们 —— 来自今日 04:0x–06:5x 的一手侦察 + 反编译解锁 + 四透镜读码。

### 🔴 今日主线 1：三靶 subtlety 猎杀收口（big-scope-subtlety-hunt 三路 Lens A/B/C）→ 全 CLEAN
对已 CLEAN 的三靶做组合面收口猎杀（找四透镜可能漏的 subtlety）：
- **gammaswap**（Immunefi $40K）：读 CPMMMath(478)/FullMath(322)/CPMMBaseStrategy/BorrowStrategy/BaseStrategy —— `checkMargin` 从实际余额重算不变式、FullMath 全 floor、LP 转换 ceil 守卫、`withdrawFromCFMM` 走 UniV2 burn。Lens B(签名) N/A。→ **CLEAN 维持**。
- **extrafinance**（Immunefi $100K）：重读 VeloPositionManager —— `payToVaultCallback`/`payFeeToTreasuryCallback` 强制 `vault==_msgSender()`、主入口透传 `_msgSender()` 归属校验、liquidation/rangeStop 受 whitelist。Lens C 核心 math 在 VeloVault(PI-only) GAP 维持。→ **CLEAN + VeloVault GAP 维持**。
- **usdn**（Immunefi $50K）：重读同构残面 `Usdn4626`(实际余额增量法抗通胀) + `Usdnr`(1:1+yield 流向锁定)。→ **CLEAN 维持**。
- **结论**：三靶三路 Lens 全部 CLEAN，无通过 PoC 的 bug → 铁律⑩ **不提交**。

### 🔴 今日主线 2：全平台新靶侦察（一手 curl，防假阴性）→ 真实空窗
- **Immunefi**：246 program 全量 API → `scan_since_0831.py` 筛 recency≥2026-08-31 → **0**（最新合规候选 Folks Finance=2026-03-11，198d）。
- **Sherlock**：全 31 页 301 contest 枚举 → 非 FINISHED=**0**（当前零活跃/即将开赛）。
- **CodeHawks**：45 contest → 非 finalised=**0**。
- **Cantina**：改企业级 AI 审单，无研究员自助池 → 排除。
- **GitHub 上游**：9 桶近 60d push Solidity → 仅 0–2 星学生/NFT/AI 仓，无协议级新鲜真靶。
- **结论**：金标自 8/31 空窗跨三平台交叉验证 = **真实空窗，非侦察遗漏**。落盘 `bounty-recon/RECON_2026-09-26.md`。

### 🔴 今日主线 3：平台规则/范围变更一手核验（元铁律⑲ 落地首核）
- **新上线靶**：Immunefi 全量最新 launchDate = 2026-08-18（`audit-competition-ens`，审计竞赛类非标准赏金）；**0 个 program 在 2026-09-01 后上线** → 9 月无任何新靶（空窗实际已 ~39 天，比"8/31 起"更长）。
- **规则/范围变更**：近 7 天（≥9/18）16 个 program 改过规则/范围。逐条过铁律③（NoKYC+Solidity+≤$100K+EVM）：
  - 排除（KYC/红海/暂停/非EVM/蓝筹）：cosmos / horizen / hedera / hathornetwork(paused) / arbitrum / cowprotocol / rootstocklabs / zksync-os / zerion / 0x / sky(死路) / enzymefinance($200K 超帽)。
  - **dhedge**：NoKYC $50K Solidity 多链，但 **audits=11**（Sherlock/iosiro 等至 2025-09）→ 红海，铁律③毙。
  - **ipor**：NoKYC Solidity ETH/Arbitrum，但 max=$1000（过低，仅练手非挣钱主线），audits=0 → 低优先。
  - **pareto（Pareto Credit）**：**NoKYC $50K + Solidity + ETH + audits=0 + 27 assets** → 唯一真正"挣钱新方向"候选；但 **githubUrl=None（无公开仓库）** + launchDate=2021（成熟协议）。需先确认 Etherscan 源码可程序化取得方能进 GOLD 深审。
- **4 个在跟免押靶条款无变动**：extrafinance(kyc=F,max=100k,upd 9/16 条款未变) / usdn(kyc=F,max=50k,upd 7/24) / staderforeth(kyc=F,max=1M,upd 2025) / orderlynetwork(kyc=F,max=100k,upd 2024) → 核心条款（kyc/maxBounty）均无利空/利好改动。

### 🔴 今日主线 4：VeloVault 闭源 math 反编译解锁（extrafinance 真残面落地）
- **关键发现**：所谓"PI-only 缺源"= **5 个 VaultLibrary 合约在 Optimism Etherscan 全部未验证**（10/10 地址 `getsourcecode` 返回空），克隆仓库也无库 `.sol`；dispatcher VeloPositionManager(0xf9cf) 本身已验证（对照证明 fetch 正常）→ 用户走 Edge 也取不到源码。
- **突破（可复用方法论）**：`eth_getCode`（Optimism RPC `https://mainnet.optimism.io`）取 10 个库字节码落盘 `velovault_bytecode/*.hex` → `panoramix-decompiler==0.6.1`（managed venv）反编译。
- **致命坑 & 补丁**：Windows 无 SIGALRM → panoramix 的 `timeout_decorator.timeout` 在每函数抛 AttributeError，致 5 个核心 math 函数全标 failed。须先 monkeypatch 成 no-op（接受 `timeout_exception` kwarg）再 `decompile_bytecode(bc)`，否则核心函数解不出（patch 后 failed=0，产出 349KB）。
- **产出**：`VaultPositionLogicLib_s0.dec.txt`（5 核心函数 5ec2a098/8c7b6f25/bce606ef/e39867d5/fd9c90ab，体量 104/781/405/2388/593 行）→ 提取 `VaultPositionLogicLib_s0_core5.txt`。
- **已读 e39867d5(2388 行) 前 400 行**：delegatecall 上下文守卫(`require this≠lib`)、owner 属主校验(`field_0==cd[36]`)、回调 manager(0x57dc7e88)、借款额度信封(stor14/stor18)、预言机陈旧分支(stor13/17 阈值：新鲜价仅 premium 可借，陈旧降级 getCurrentDebt)、全程 checked 算术。**前 400 行未发现明显漏洞；协议成熟 + PeckShield 已审 → EV 中等，decompiled 结论须经 on-chain PoC 方可声称 bug（铁律⑩）**。
- **待办**：剩余 ~4000 行（清算奖励/bad-debt math 等）深审。详见 `bounty-recon/extrafinance/audit-notes.md` §9。

### 🔴 今日主线 5：Pareto Credit 源码拉取 + 四透镜深审（空窗期唯一浮现的新候选）
- **源码可及性 ✅**：26/26 资产合约全部 Etherscan V2 已验证，源码完整可取 —— 彻底排除"缺源"担忧。
- **资产构成**：27 assets = 19 个 TransparentUpgradeableProxy + 7 个直接实现 IdleCDOTranche。19 代理背后**仅 8 个唯一 implementation**（去重）。
- **核心逻辑 3 合约**：`IdleCreditVault` 主实现 0x55573(1864 行) / `IdleCDOEpochVariant` 主实现 0xf70e98(4693 行，内联 Tranche+CreditVault base+IdleCDO base) / `IdleCDOEpochQueue` 0x492af7(109 行)。其余 5 个为 delegatecall 薄壳。
- **Pareto 定制铁证**：合约名硬编码 "Pareto Credit Vault" / "Pareto AA/BB" → 非纯 Idle 标准，是 Idle CDO 框架的 Pareto 部署/定制。
- **攻击面定位**：CreditVault 全部 value-moving 函数强制 `_onlyIdleCDO()` ⇒ 普通用户不能直达，须经 **IdleCDO/EpochVariant(0xf70e98) 中枢**。
- **已读**：CreditVault 全函数 + IdleCDO base + EpochVariant 的 startEpoch/stopEpoch/depositDuringEpoch/requestWithdraw/_withdraw/_liquidate/_checkDefault/_updateSplitRatio/_calcInterest*。
- **框架防护较全**：nonReentrant / `_checkDefault`(price drop revert) / `_checkSameBlock` / paused / GuardedLaunch(`_guarded` limit) 皆在。
- **3 个待 PoC 证伪疑点（非确认 bug，铁律⑩）**：
  - **B（较高危）requestWithdraw 的 instant-withdraw-on-APR-drop 分支(4453-4469)**：`_amount==0` 时 `_underlyings=_trancheToUnderlyings(_userTrancheTokens)`（含已结算利息），instant 分支用含息金额给用户，但设计意图 instant=放弃利息 → 待 PoC 是否致 AA/BB 失衡/超付。
  - **C（较高危）depositDuringEpoch 定价(4361-4427)**：mid-epoch 定价 `_minted=(_amount+trancheInterest)*supply/expectedFinal`，仅查 `supply==0` 未查 `expectedFinal==0`；待 PoC 闪电贷拉抬/压低 tranchePrice 致定价偏差 + 时序套利。
  - **A（低危）epochNumber 时序**：stopEpoch 在 isEpochRunning 仍 true 时调 `_strategy.deposit(netInterest)`(4192) → CreditVault.deposit(1763) 因 isEpochRunning 触发 `epochNumber+=1; totEpochDeposits=0` → 闭环平衡不损用户资金，暂判低危。
- **评级**：铁律③ 完美命中 GOLD 甜区（免押/NoKYC/$50K/0审计/9-16 新 scope），但底层 = Idle 成熟框架 + 仅定制层 ⇒ **EV 中等（靠定制面出 bug）**；audits=0 + 新 scope = 当前竞争低，是入场窗口。
- **落盘**：`bounty-recon/pareto_audit-notes.md`（架构/跨合约图谱/疑点/待读清单/评级）；源码已存 `bounty-recon/pareto_src/*.sol` + `IMPL_*.sol`。

### 🔴 今日主线 5 续：Pareto 二轮深审收口（逐字对质证伪）→ **CLEAN，retire**
- 用户拍 `1`（搭 fork 证伪 B/C）。**精读后 B/C 均为设计行为，非 bug**：
  - **B**=instant-withdraw-on-APR-drop 含息(4433-4497)：instant = 立即按含息价赎回（tranche 本含息），`_withdrawOps` 减 NAV 精确匹配，公平非超付 → CLEAN。
  - **C**=depositDuringEpoch 定价(4361-4427)：基于**确定性终点 NAV 公式**（lastNAV+expectedInterest），不用实时 tranchePrice → 闪电贷无法操纵 → CLEAN。
  - **A**=epochNumber 时序(4192/1763)：闭环平衡不损资金 → 低危确认 CLEAN。
- **更高危面（epoch/default/NAV）全 owner/manager/rebalancer 守卫 + 会计自洽**：`stopEpoch` public 但 `_checkOnlyOwnerOrManager()`(4129，初判"红旗"系误读)；`getContractValue` NAV 依赖 CreditVault 账本，非特权不可操纵。
- **收口裁定**：Pareto Credit 四透镜深审**全 CLEAN**，无符合铁律⑩（非特权可触发）的可提交 finding；retire 为"已深审 CLEAN（静态）"靶，**不写 fork PoC（证无 bug 负 EV）**。

### 📌 铁律⑯ 本轮 NOW 收口（元铁律⑲ 应用 — 重定向资源）
- **过程**：本轮先按"重定向资源"把 NOW 定为 **pareto 四透镜深审（含 PoC）** —— VeloVault（反编译 + 成熟 + PeckShield 已审）与三 CLEAN 老靶 EV 均低于 pareto。
- **二轮纠偏（元铁律⑲）**：主人选 `1` 是基于"B/C 可能真 bug"的假设，但精读**逐字对质后证伪为 CLEAN**（B/C/A 全为设计行为）。继续搭 fork 证无 bug = 浪费资源 → **retire pareto**。
- **现在最该做且只做一件**：**静默监控金标**（空窗未破 + 无合规新靶，强行深审 = 浪费；HOURLY 金标侦察自动化 `1790201448486` + 每日 07:30 已覆盖，新靶出现即 Discord 告警）。
- **纠偏依据**：三靶均为重度审计/老靶，多轮复核均 CLEAN → 继续刷 EV≈0；空窗期继续刷 CLEAN 老靶 / 扒 junk GitHub leads = 负 EV。
- **显式 defer**：① VeloVault 剩余 ~4000 行 grind（EV 低于 pareto 且 pareto 已 CLEAN，暂缓）；② dhedge(audits=11 红海) / ipor($1K 过低) 排除；③ pareto 已 retire；④ 其余练手靶全 CLEAN 归档。
- **系统安全核验**：全程纯本地白帽分析 + 只读 curl/RPC + 本地反编译，**零登录 / 零提交 / 零私钥接触**。

### 📌 承接与待办
- **金标空窗连续自 8/31（系列第 35 天 / 自 8/31 计 26 天）**：铁律⑪ 维持静默监控；automation `1790201448486` HOURLY 跑 Immunefi 单源 + Discord 告警。
- **真靶**：TermMax V2 仍唯一 LIVE/ready（等 soldeer + mainnet-fork PoC + 用户 Edge 提交）；Twyne 转永久 dead；Midas 结案 / Tenor #407 parked（锁死真因已查实）/ Hinkal 不提交；orderly·gammaswap·usdn·staderforeth 四透镜 CLEAN。
- **新靶候选**：**pareto（Pareto Credit）** = 空窗期唯一浮现的新候选，源码已取 + 两轮四透镜深审 **全 CLEAN → retire**（不写 fork PoC）。
- **环境限制记录**：沙箱内跑完整 gold_pipeline 会被进程组 kill（分源跑规避）；WSL 被安全策略禁用（须用户在安全中心开启）；panoramix 在 Windows 须 SIGALRM 补丁。
