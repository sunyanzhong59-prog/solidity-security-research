# 2026-09-20（Day 67）

## 今日状态

### 🎯 主战线：空档第 29 天（GOLD 持续 0）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：Immunefi 原始快照已更新（`immunefi_bounties.json` 06:06）→ **三源全空窗**（Immunefi 金标 0，`dead_filtered: mtpelerin / twyne / synthetix`；Sherlock 0；CodeHawks 0）；Cantina 每日侦察 06:40 已跑（见主线 3，0 新赛）。**今日 07:30 金标流水线 / 09:00 三源侦察档的产物（`scope-diff-2026-09-20.md` / `competitions-recon-2026-09-20.md`）落笔时尚未生成**，待跑完后核对。
- **9/19 侦察基线**：Immunefi 新增 bounty 0 / 下架 0 / maxBounty 变更 0；72h 新资产 79（Aera 15 条 + Spark 系列 + Pareto Credit vault 扩容），**全部为既有 program 的资产扩容 / 新增合约，非新 bounty** → 不产生新竞争窗口。
- 空档纪律（铁律⑪）：练手仅在用户明确说「练」时开；否则静默监控金标，想干真活就深审 parked 真靶。

### 🔴 今日主线 1：alejo-vault Foundry PoC 闭环 —— 5/5 PASS，五发现实测分类完成
- **背景**：9/19 读码在 `alejoacosta74/ethereum-erc20token-vault`（296 行，ETH 抵押稳定币借贷，github-fresh 脏代码）列出 5 个「疑似漏洞」；今日按用户指令把它们全部写成**可运行 PoC**，落盘 `bounty-recon/practice/alejo-vault/foundry-poc/`。
- **工程要点（已回填 MEMORY.md「PoC 工程复用坑」）**：
  - 本地 OZ 库是 **v5.6.1**，`Ownable(address initialOwner)` 必传参 → 给 `Coin.sol` / `Vault.sol` 构造函数补 `Ownable(msg.sender)`（原始为 v4 风格），否则 Error(3415)。
  - **零成本假喂价**：合约硬编码 Kovan 聚合器地址（`0x9326...`）→ 用 `vm.etch(ADDR, address(FakePriceFeed).code)` 注入假 feed runtime 字节码，**`PriceConsumerV3` 原码一字未改**。
  - 无 forge-std：内嵌最小 `Vm` 接口（etch/deal/prank/expectRevert）指向 HEVM 地址。
  - 测试合约必须 `receive() external payable {}`，否则 `_to.transfer()` 退回 ETH 时反悔（F1/F4 会因此假失败）。
  - **重入探针方法学修正**：`.transfer()` 仅 2300-gas，真重入无法在 `receive()` 内记录状态 → 改用**「顺序双取」**证明 checks-effects 已阻断双花。
- **`forge test` 结果 5/5 PASS，实测分类（关键：5 个疑似里只有 1 个是真 bug）**：
  | # | 疑似漏洞 | 实测裁决 | 依据 |
  |---|---|---|---|
  | F1 | `withdraw()` 无 `nonReentrant` → 重入 | **非 bug** | burn 在 transfer 之前 = checks-effects 已救；再加 2300-gas 纵深 |
  | F2 | 价格下跌 → 取回下溢锁资 | **真 bug（Medium）** | 稳定币还原、**ETH 永久冻结**（失访 DoS）；若 `unchecked`/pre-0.8 则升级为可抽干；预言机可操纵时升 High |
  | F3 | `MockOracle.setPrice()` 无访问控制 | **条件性（Low）** | 仅 Kovan 测试网，**不在 Vault 生产路径** |
  | F4 | 整数除法舍入截断 | **真但可忽略（Info）** | 机制真、单笔 sub-wei dust 锁死，不构成可报损失 |
  | F5 | 预言机零值 → DoS | **真 DoS（Low/Medium）** | 存入铸 0 锁资（静默损失）+ 赎回除零 revert，区分两类后果 |
- **训练价值（本日最重要产出）**：强制校准 spotting 的**双向偏置**——既别过度判非 bug（F2 就是真 bug），也别过度判 bug（F1 有 nonReentrant 缺失但机制上安全、F4 机制真但无损失）。
- 报告：`bounty-recon/practice/alejo-vault/foundry-poc/AUDIT_REPORT.md`

### 🔴 今日主线 2：五发现合规报告练习 → 覆盖「五类写法」，沉淀 `bounty-report-writer` skill
- 把 F2 单独按 Immunefi / Cantina / CodeHawks 格式写成**练手报告** `BOUNTY_REPORT_F2.md`，再补齐 F1/F3/F4/F5 各一份九段式报告，**五份合起来覆盖五类写法**：
  - **F2 = 真 bug 完整报**：关键准确点是 Solidity 0.8 下溢 = **panic revert（非回绕）** → 用户稳定币能还原，但 **ETH 永久冻结**；Severity 判 **Medium**（市场条件触发），预言机可操纵时升 **High**。
  - **F1 = 证伪记录**：缺 `nonReentrant` 但 checks-effects + 2300-gas 已救 → 明确写 **None**，证伪也要留档。
  - **F3 = 条件性先看攻击面**：`setPrice` 无权限确实存在，但脱钩 Vault 生产路径 → Low。
  - **F4 = 真 bug 不等于要报**：sub-wei dust 锁死机制为真 → Info。
  - **F5 = DoS 必写前提与两类后果**：静默损失（铸 0）vs revert（除零），看喂价能否为 0 → Low/Medium。
- **沉淀**：报告结构模板已固化为用户级 skill **`bounty-report-writer`**（九段结构 + severity rubric + 从 PoC 派生攻击场景 + 「练手不提交」标注纪律）。
- **纪律**：以上全部为**练手**，靶不在任何合规赏金 scope 内 → **一律不提交（铁律⑩）**。

### 🔴 今日主线 3：Cantina 每日侦察（06:40 运行）→ 0 新赛；并发现 diff 脚本分类 bug
- Edge 活体渲染 `cantina.xyz/opportunities`（SPA，`channel: msedge`）。页面头部实读：**Bounties 51 / Competitions / Ended 144**，payouts available **$65.1M**，total paid out **$54.8M**，Vulnerabilities found **7,763**，Researchers **22,987**。
- **51 个 live UUID 与 9/19 基线逐一核对：完全一致 → 0 新赛、0 消失**（`newUuids=[]`）。已排除「误读 Ended 标签」失败模式（确认默认视图即 LIVE 赏金）。按任务步骤④对「无新赛」静默处理，不群发告警。
- **⚠️ 工具 bug（需修）**：`cantina_diff_summary.json` 输出 `liveCount: 0 / endedCount: 51 / missingNowEnded: 51 条`，与事实不符。根因 = 探针把**整页文本**塞进了每个 card 的 `text` 字段（card 文本里含 "All 51 Bounties … Ended 144"），于是「ended」关键词启发式判定**全卡命中**。**结论必须以 UUID 集合人工核对为准**（本次已人工核过 = 0 新赛），`cantina_diff.js` 的 ended 判定逻辑待重写（应改为按 card 内独立区块/`href` 解析，而非整页关键词匹配）。
- 侦察产物：`bounty-recon/cantina_probe_out.json`、`cantina_diff_summary.json`（原始证据留档）。
- **年轻池（<90d，EVM，均非新，且全部 Deposit required + KYC）**：Robinhood $1M / 10d / $30 押金 / 88 findings；Tenbin $40K / 8d / $50 / 71；Ammalgam $25K / 71d / $50 / 74；Phantom $100K / 80d / $5 / 196（scope 含 Solana/web 存疑）；Rogo $25K / 85d（web/iOS → 非 Solidity 排除）→ **无一满足「无押金 + 无 KYC」**。

### 📌 真靶战线现状（承接 9/19，无新增变动）
- **Midas #122 → 已结案**：#355 + #356 **双 Duplicate**，$40 押金全退、零亏损、零奖金。**不再对该 program 投资本**。
- **TENOR-001（Sherlock #407）→ parked（经济通道锁死）**：提交流严重度只有 批判/高 两档且全需 250 USDC 存款，**无 Medium 档**；Tenor 官方安全页明示 bounty 只跑 Sherlock、无免费镜像。技术上仍有效，但**明确不值得为此投资**。
- **Hinkal（HackenProof）→ 不提交**：四透镜深审无提交级候选（最深仅 Info/Low，且合约常态零余额）→ EV=0，按铁律⑩不提交（省 amlbot KYC 触发与精力）。
- **✗ 不为「有靶打」放宽过滤器**：Alchemix v3（EVM+免费+$150K，但上线 ≈204 天超 90 天红线）→ 不放宽。

### 🧪 练手资产（空档期沉淀，零 bounty EV、一律不提交）
- **脏代码 spotting 训练（9/18 转向后主线）本日新增样本**：
  - `practice/alejo-vault/`（alejoacosta74 ETH Vault，296 行）→ 5 发现 + **Foundry PoC 5/5 PASS** + 五份合规报告练习。**校准结论：5 个疑似里 1 个真 bug（F2），spotting 双向偏置已显式校准。**
  - `practice/nico-lending/`（NicoMM8，11 合约 / 1329 行教育级）→ 四透镜后**无 High/Critical 可提交漏洞**；收益 = 确认 OZ 规范库的残留漏洞需**跨交易预言机操纵 / 治理级攻击**，纯读码难发现，必须 Foundry 模拟攻击路径。
  - `practice/blaze-phoenix/`（Blaze Phoenix DEX，5 `.sol` / ~8730 行，V2/V3/V4/Solidly/Algebra 聚合器）→ 四透镜全 PASS：Router 不持资金且无 approve（扎实）、EIP-1153 transient reentrancy lock + TSLOT_FEE 双重费保护、FoT 非对称处理为已知权衡。**代码质量极高、19 credited researchers、零 Critical → 无残留漏洞，不提交。** 另记：其 bounty 约 50M BZPX，但 **BZPX 非稳定币且 2026-10 前不发放 = 现金价值不确定** → 优质学习材料，不适合现阶段变现投入。
- **盲卡库 38 张**：`total=38 / 冷审通过=38（全 4/4 PASS）/ Critical=15 / High=22 / Medium=1 / 覆盖族=38 / 结构化答案键=32`。面板 `bounty-recon/live-audit/blind-coverage-matrix.html`。
- **其余靶**：`practice/siyam-perp/`（Critical 资金锁 4/4 PASS）/ `practice/maxwire-staking/`（诚实证伪对照 5/5 PASS）/ `practice/hinkal-revisit/` / `practice/starknet-l1-solidity/` / DeFi Saver V3（R1–R8 全 FALSIFIED，44 tests PASS）。
- **待令靶**：YieldNest Max Yield Vault（`practice/yieldnest-vault/`，audit-notes 骨架已写）。**未获「练」指令前不动，严禁提交任何 finding（铁律⑩）。**

### 💰 提交闭环真实成本（继续记牢）
- **不能说「流程走通 = 成功」**：Mezo #346 $50 押金（Polygon）已押死 + finding 悬 + 评审未出 = 沉没；Immunefi **Novice 100 USDC / program** 是 program 级闸门（1inch Aqua / GMX / Chainlink 等疑似同组）。
- **纪律**：真靶筛选优先**无 Novice fee** 的 program；收费则 100 USDC 须纳入 EV；真变现须先确认 **withdraw 路径**（铁律⑥），**零 EV 靶绝不押钱**。
- **当前唯一零障碍通道**：**Immunefi $0 押金 + KYC 已通（9/19）** → 下一个真靶的首选落点。

### ⏳ Mezo Bug Bounty #346（用户指令：有消息第一时间报告）
- 状态：押金 $50 Polygon 押死（翻篇）、finding 悬、评审结果未出。自动化每 2 天查公开状态兜底；**最可靠信号 = 平台发往 sunyanzhong59@gmail.com 的邮件（主人自看邮箱）**。

### 🔒 Parked 真靶
- **Twyne（Immunefi，No-KYC）**：四透镜唯一强候选已证伪，S1–S4 全闭环无提交级 finding → 维持 parked（不花 100U）。
- **ModularAcctV2（Cantina，无押金/KYC 过）**：红海（586 findings）→ 不提交。
- **EVM Bucket（buck-labs/buck-v2）**：仍未上架 Immunefi/Cantina；上架即报（PoC + 报告已备）。

### 🔭 Track B「Rialo Researcher」（候用户指令）
- 两条路全断（Discord 入服需手机号验证 / Guarded Vault `PlaygroundError` / Predict 需 Discord 角色）；9/14 计划的三项实验**仍未执行、无新记录**。
- 稿子就位：`bounty-recon/track_b_rialo/researcher_post_1_guarded_vault.md`。**反诈骗铁律（Rialo 明令）：Discord 验证绝不连钱包。**

### 📅 日历节点核对（今日无可用新赛）
- **Sherlock Fusaka $2M（9/15–10/13 LIVE）**：以太坊核心协议升级类，**非 DeFi 合约** → 维持排除。
- **ENS Audit Comp**：9/14 已截止（scope 为 137K 行 TypeScript）→ 排除。
- **HackenProof**：Hotcoin（NEW 9/14，CEX/WEB 非合约）/ Bullbit、Zeko、Zynk 均 PAUSED → 无新免费 EVM 小池；声誉点 + KYC 提现墙封死免费面。
- **CodeHawks**：无新付费赛（BattleChain 7/16 已结束；First Flights = AI 练习层无奖金）。
- 倒计时全失效：Sentiment（8/29 已证伪不存在）/ Aero-MetaDEX（8/31 锁死）无开赛意义。
- **当前无有效「即将开赛」免费/低押金小型 EVM 竞赛**；真瓶颈 = **deal flow（新鲜低竞争靶）**——KYC-for-payout 在 Immunefi 侧已解除。

## 备注
- 经济原则不变：只做免费/零押金项目；任何花钱一律等确定回报再报用户 100% 确认（铁律④⑥⑪ + USER.md 第 9/10/11 条）。
- 今日无任何资金动作（无押金、无充值、无买币），Cantina 余额维持 **$50**，账面零变化。
- 例行：GitHub commit、D 盘备份。
- **今日关键进展一句话**：**alejo-vault 五发现从「疑似」走到「可运行 PoC + 合规报告」闭环（5/5 PASS）**——实测 5 个疑似里**仅 F2 是真 bug（价格跌 → ETH 永久冻结，Medium）**，F1 非 bug / F3 条件性 / F4 可忽略 / F5 带前提 DoS，**强制校准了 spotting 的双向偏置**；同时 Cantina 每日侦察确认 **0 新赛**（51 live UUID 与基线一致），并发现 `cantina_diff.js` 的 ended 判定因整页文本误判而失效（**结论须以 UUID 集合人工核对为准，脚本待修**）——**下一步主攻方向不变：Immunefi 无押金 program 的新鲜低竞争靶 + dirty fork spotting 扩样本量。**
