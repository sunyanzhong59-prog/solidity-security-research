# 2026-09-14（Day 61）

## 今日状态

### 🎯 主战线：空档第 23 天（GOLD 持续 0）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日侦察**：07:30 `gold_pipeline` 与 09:00 三源侦察（Immunefi 金标 / Sherlock / CodeHawks）按例行跑；本 note 落笔时（06:55）尚未出结果，待跑后按「命中合规靶 → 深审，否则空窗延续」处理。
- **9/13 侦察基线（三源均空）**：Immunefi `GOLD=0`（dead_filtered 持久剔除 TermMax / Mt Pelerin / Twyne / Synthetix）；Sherlock 情报 0；CodeHawks 情报 0。
- 空档纪律（铁律⑪）：练手仅在用户明确说「练」时开；否则静默监控等干净金标，想干真活就深审 parked 真靶，不把「回练手堆」当战略出路反复摆。

### 🧪 今日主线：盲卡覆盖矩阵面板（①）+ 扩未覆盖族（②）——盲卡库 34 → 38 张
- **① 覆盖矩阵面板（05:26 完成）**：产出 `bounty-recon/live-audit/blind-coverage-matrix.html`（按族 × 峰值严重度 × 冷审状态 × 答案键形式的覆盖矩阵）。
  - 数据校准坑（已修）：`glob("blind-*")` 会把输出的 `.html` 当目录匹配 → 加 `os.path.isdir` 过滤；早期卡 `REPORT.md` 是冷审模板无结构化 severity、近期卡 `solution.md` 大小写不一 → 峰值严重度统一用建卡整体定性 `SEV` 字典（不混源）；`SEV` 字典 5 张早期卡 key 缺 `blind-` 前缀 → 加 `name.replace("blind-","",1)` 容错。
  - **生成脚本已从 temp 固化到仓库**：`bounty-recon/live-audit/tools/blind_matrix_gen.py`（可一键重生成，不再是一次性临时文件）。
- **② 扩未覆盖族（05:40–06:57，新增 4 张卡）**：全部「自建 synthetic 孪生靶 + 无 forge-std + `require` 断言」范式，各 4/4 PASS。

| 卡名 | 族 | 峰值严重度 | 结果 |
|------|----|-----------|------|
| `blind-storage-collision` | 代理存储槽碰撞/布局错位 | Critical | 4/4 PASS |
| `blind-mev-sandwich` | AMM 三明治 / 订单排序 MEV | High | 4/4 PASS |
| `blind-tx-origin-auth` | tx.origin 鉴权绕过 | Critical | 4/4 PASS |
| `blind-delegatecall-inject` | 代理 delegatecall 注入夺权 | Critical | 4/4 PASS |

- **⚠️ 修复上一会话遗留的半成品卡（`blind-delegatecall-inject`）**：该卡 `src/Target.sol` + `test/BlindFind.t.sol` 已写但**编译不过**，且缺 `CHALLENGE.md` / `REPORT.md` / `src/solution.md`。两处硬错：
  1. `Evil` 合约被**嵌套声明**在 `contract BlindFind { ... }` 内部 → Solidity 0.8.19 `Error (9182): Function, variable, struct or modifier declaration expected.`
  2. `bool ok = address(t).call(...)` → `Error (7364): Different number of components on the left hand side (1) than on the right hand side (2)`，必须 `(bool ok,) = ...`。
  修复后补齐三份文档（含 6 条踩坑记录）→ 4/4 PASS。
- **盲卡库现状（重生成矩阵实测）**：**total=38 / 冷审通过=38（全 4/4）/ Critical=15 / High=22 / Medium=1 / 覆盖族=38（每族单卡，无重复族）/ 结构化答案键=32**。
- **新增踩坑（建议回填 `blind-card-template` skill）**：
  - **G9 恶意合约必须声明在文件顶层**（0.8.x 不允许合约内嵌套声明合约）——本条此前未在模板六坑（G1–G6）中列出。
  - **G10 低层调用返回值必须用元组解构**（`(bool ok,) = addr.call(...)`），单变量接会触发 7364。
  - **低层调用返回值的「警告取舍」**：`(bool ok,) = ...` 触发 unused-variable 警告，`impl.delegatecall(data);` 触发 return-value-not-used 警告——若「丢弃 `ok` 不 revert」本身就是漏洞形态，**应保留警告而非加 `require(ok)` 改变挑战语义**，并在 `solution.md` 里显式说明。

### 💰 提交闭环真实成本（9/12 纠错，继续记牢）
- **不能说「流程走通 = 成功」**：Mezo #346 $50 押金（Polygon）已押死 + finding 悬 + 评审未出 = 沉没；Immunefi **Novice 100 USDC / program** 是新增闸门（program 级，1inch Aqua / GMX / Chainlink 等疑似同组）；Ethena 亦提交失败过。
- **纪律更新**：真靶筛选新增「**优先选无 Novice fee 的 program**」；若目标 program 收费，100 USDC 须纳入 EV 计算；真变现须先确认 **withdraw 路径**（铁律⑥）再谈提交，**零 EV 靶绝不押钱**。

### ⏳ Mezo Bug Bounty #346（用户指令：有消息第一时间报告）
- 状态：押金 $50 Polygon 押死（翻篇）、finding 悬、评审结果未出。自动化每 2 天查公开状态兜底；**最可靠信号 = 平台发往 sunyanzhong59@gmail.com 的邮件（主人自看邮箱）**。

### 🔒 Parked 真靶
- **Twyne（Immunefi，No-KYC）**：9/11 四透镜深审唯一强候选（Morpho shares-repay 向上取整 + 双 accrual 不一致）已**证伪**；9/13 本机真主网分叉机器测试（`refundsDust` 系列 4/4 全绿 + `-vvvv` trace 确认走退款分支）完成 **S1–S4 全闭环**，无提交级 finding，**按铁律⑥不提交（100U 纯沉没）**，维持 parked。9/12 一手核实 yAudit 报告索引确认 **#46(beb4bf1) 未被公开覆盖**（gitbook 裸文件引用 ≠ 审计证据）。
- **ModularAcctV2（Cantina，无押金/KYC 过）**：红海（586 findings），建议不提交。

### 🔭 Track B「Rialo Researcher」（候用户指令）
- 9/13 两条路全断（Discord 入服需手机号验证、Guarded Vault `PlaygroundError`、Predict 需 Discord 角色），用户决策「今天撤、明天再实验」。
- **9/14 实验清单**：① Discord 换入口/换号/隔日重试（验证服务有时限流）；② Guarded Vault `PlaygroundError` 刷新重测（余额 7 $RIALO 已够）；③ 进服后立刻把 `bounty-recon/track_b_rialo/researcher_post_1_guarded_vault.md` 发到 `#research` 冲 Researcher 角色（最高价值、零阻塞）。**反诈骗铁律（Rialo 明令）：Discord 验证绝不连钱包。**

## 备注
- 经济原则不变：只做免费/零押金项目；任何花钱一律等确定回报再报用户 100% 确认（铁律④⑥）。
- Human Passport 26 分（满权重）已定案，与赏金 KYC 无关，不再为冲分花钱（ROI=0）。
- 例行：GitHub commit、D 盘备份。
