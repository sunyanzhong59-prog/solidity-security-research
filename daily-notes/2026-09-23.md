# 2026-09-23（Day 70）

## 今日状态

### 🎯 主战线：空档第 32 天（今日已做四平台一手活体核 → GOLD=0 为真实空窗）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：今日 07:30 金标流水线 / 09:00 三源侦察档产物（`scope-diff-2026-09-23.md` / `competitions-recon-2026-09-23.md`）均未生成，待跑完后核对；今日「空窗」结论**不依赖**它们，而是来自下面主线 1 的四平台一手核。

### 🔴 今日主线 1：四平台一手活体金标侦察（04:0x 起，铁律①⑫）→ 空窗为真，非脚本漏捞
- **Immunefi（RSC flight payload，最稳路径）**：`curl -sL -H "RSC: 1" -H "Accept: text/x-component" https://immunefi.com/explore/` → 231KB payload；写 `parse_immunefi_rsc.py`（flight 格式全局 brace-match 会被字符串干扰 → 改「定位 slug → 回找最近 `{` → 前向 brace-match」，提取 172 个程序对象）。
- **过滤（NoKYC + EVM + ≤$100K）= 33 候选**，但 **explore payload 不含 assets URL → 链类型 UNKNOWN**，必须逐条详情页核。
- **详情页核 top4（写 `parse_detail.py`）**：`gmtrade`=**Solana**（gmx-solana / explorer.solana.com）、`stackingdao`=**Stacks**（hiro.so）、`zest-protocol-v2`=**Stacks**、`pareto`=**EVM 但 2021 老靶**。
- **结论：EVM + 新鲜（≤120d 上线）+ NoKYC + ≤$100K = 0**。Immunefi 当前「新鲜 NoKYC」段已被 **Solana / Stacks 霸占**（gmtrade 78d/Solana、zest 249d/Stacks、stackingdao 2024/Stacks）。
- **Cantina（Edge 一手 SPA，playwright-core `channel:'msedge'`）**：`cantina_probe.js` 抓 `cantina.xyz/opportunities` → **51 活赏金**。过滤后 **无押金(no-deposit) EVM 活赏金 = 只有已审过的**（ModularAcctV2 / Alchemy $100K、Liquity BOLD、Agglayer）**或老靶**（USDai $100K，2025-06）**或 Private**（Paxos）；**所有新鲜靶全标 `Deposit required`**（主钱包 ~$7 + 41 USDC 付不起）→ **0 免费新鲜 EVM**。
- **CodeHawks（Edge）**：`multi_probe.js` 抓 first-flights + contests → **First Flights 全 Ended**（最新 #59 SNARKeling 已结束 152d）、**Competitive Audits 全 Ended**（最新 BattleChain 68d）→ **0 活**。
- **Sherlock（Edge）**：`app.sherlock.xyz/overview` 显示 `COVERED PROTOCOLS 0` → **0 活跃审计覆盖**。
- **总判定**：四平台（RSC + Edge）交叉验证 → **GOLD=0 空窗为真实（当下确无「新鲜+低竞争+EVM+免费/小池」可打靶），非 `scan_github_fresh` 脚本缺陷**（该两缺陷仅影响 github-fresh 上游桶，不影响平台金标扫描）。**空窗纪律维持（铁律⑪）**。
- **铁律⑫/⑬ 诚实边界**：私有 submissions/triage 页（hunter 密度、同根因查重）需用户 Edge 登录态手核——沙箱无登录态、Cloudflare 拦无头；但**公开 listing 证据已足以判空窗**，无需读私有页即可下「无靶」结论。
- **产出文件**：`if_explore_rsc_2026-09-23.txt`、`if_det_{gmtrade,zest-protocol-v2,pareto,stackingdao}.txt`、`immunefi_filtered_2026-09-23.json`、`parse_immunefi_rsc.py`、`parse_detail.py`、`cantina_probe.js`、`multi_probe.js`。

### 🔴 今日主线 2：gold_pipeline 手动复跑（05:29）交叉印证空窗
- 触发：用户「现在手动跑一次金标侦察确认当下 0 靶？」→ 跑 `gold_pipeline.py`（默认 main：scan_gold Immunefi 一手 + scan_sherlock / scan_codehawks 情报，各 90s 超时护栏）。托管 python 全路径、`PYTHONUTF8=1`、`.env` 已载，EXIT=0，耗时 2m21s。
- **核心结论行**：`[pipeline] gold(Immunefi 可提交)=0 killed=0 clean_candidates=0 intel(Sherlock/CodeHawks 情报)=0` → **0 合规可提交靶**，与主线 1 四平台一手核一致。
- **噪声口径澄清（防误读）**：① 末尾一批 `-> !! CANDIDATE ... promote=False` 是 Immunefi program 名称 / owner 模糊命中（Ether.fi / 1inch / Chainlink / AAVE / Polygon 等），脚本标「scope 未确认·仅候选不自动升 GOLD」→ **非已确认靶**（铁律②/⑤ 不计入）；② `whitelist GOLD match fresh=7 matches=2`（Uniswap / Morpho 等）是白名单 org 新鲜仓**练手种子**，非 Immunefi 赏金靶。
- 结论：自动化 18:00（现 17:30）档继续监控即可，当下无需手动干预。

### 🔴 今日主线 3：铁律⑪ 口径澄清（用户质疑「停放真靶为何还复查死活」）
- **结论**：已停放（dead）真靶**不复查死活**——冗余，且违 HP_DEAD_SKIP / 铁律⑪「不重复浪费注意力在死路」。
- **两件事拆清**：①「复查死活」= ping 平台看程序活不活 → 对 parked 靶纯冗余（活/死都不交，根因已定）；②「深审 parked 真靶」= 拿已克隆真实代码练残留证伪 / 第二层复扫 / 四透镜再一遍（练手长肌肉，不花平台注意力），**≠ 等复活**。
- **唯一例外**：停放因**可逆临时条件**（如 Tenor 锁死解锁、Hinkal 改交 HP）才可能再碰，且监控的是「该可逆条件变了没」而非「程序死活」。当前 **Twyne / Midas / Tenor / Hinkal 全永久死，无例外**。
- **措辞修正**：「维持 parked 死局/监控」应读作「parked 维持死局、仅作练手深审对象，不再 ping 状态」；真正持续监控的是**新金标（活靶）**。

### 🔴 今日主线 4：非 EVM 转赛道评估（用户 5mo 0 收入、亏 100+U，考虑 pivot Solana / Stacks）
- 用户诉求：评估转非 EVM（Rust / Clarity）审计可行性 + 学习成本 + 首笔收入时间线；引今日主线 1 侦察「gmtrade / stackingdao / zest 都新鲜」为转赛道理由。
- **市场事实（今日实锤）**：Immunefi 新鲜 NoKYC ≤$100K 段 **EVM=0**，但 Solana / Stacks 有 `gmtrade`(78d/Solana)、`zest-protocol-v2`(249d/Stacks)、`stackingdao`(Stacks) 活靶；且用户 Immunefi KYC 已验证(9/19) → **KYC 类 Immunefi 靶也能打** → 非 EVM 机会面比「新鲜 NoKYC 小池」更大。
- **关键诚实点**：用户 EVM 5mo 0 收入的瓶颈是「**真实协议应用层**」（记忆铁律已载，非语言），转 Solana 会原样搬此瓶颈 + **再加语言/运行时门槛** → 不能指望换语言就快赚钱。
- **学习成本**：**Clarity 极低**（2–3 周能审）但盘子小、奖金低；**Rust + Solana/Anchor 中高**（6–10 周能审计，本地 validator 吃 RAM）。
- **时间线（假设每日 2–4h）**：Clarity 首笔 **2–4mo** 但金额小（<$1K）；Solana 首笔保守 **4–6mo**（若撞上新鲜未审程序可缩至 3mo）。
- **推荐混合路径（最低机会成本）**：EVM 自动化监控继续白嫖（0 成本）；并行学 Solana/Anchor + 顺手 Clarity；等新鲜低竞争 Solana 程序出现再重兵压上；**别全切**（EVM 仍是总奖金最大盘，5mo 代码阅读能力有复利）。
- **红旗**：Rust 学习可能慢于预期；本地 validator 资源门槛；首笔收入仍不确定（长尾分布）；经济紧，需扛学习期 0 收入。
- **机器 RAM 实测（PowerShell → 文件读出）**：物理内存 **15.94 GB**（略低于 16G 阈值）→ 修正上条「16G+」红旗为**琥珀级**：`solana-test-validator`（审计 PoC 用）8–16G 即可跑小程序，够用；全节点 mainnet validator 需 256G+ 与审计无关。真实空闲约 11–12GB（Win + 后台吃 3–5G），跑前清 Chrome 残留 + `--limit-ledger-size` caps 账本即可。**机器非障碍**。

### 🔴 今日主线 5：换机 vs 本机学（06:04 → 06:11 用户改主意）
- 06:04 用户问「换另一台电脑学非 EVM 行不」→ **行**：学习期（读代码 + 学语言 + 方法论）对机器零硬要求，不需 validator、不需本机 15.94G；前两周纯读代码甚至不需装工具链。可移植资产 = `~/.workbuddy/skills/` 的 audit-* 方法论系列（最值钱）+ MEMORY.md / 每日日志 + `audit-notes/` + `bounty-recon/` 脚本；EVM 17:30 自动化留本机继续跑（混合路径）。红线：私钥 / seed 绝不移机；另一台登录用 Edge（铁律⑨）。
- **06:11 用户改主意：在本机学**。环境实测：**C 盘剩 22.96 GB**（够装 Rust + Solana + Anchor 3–8GB）；`rustc` / `solana` / `anchor` **均未装**（干净安装无冲突）；node 在托管路径（PS PATH 未扫到，非缺失）。→ **本机学无实质冲突**：装工具尊重 D 盘惯例（CARGO_HOME / RUSTUP_HOME / Solana 装 D），RAM 15.94G 学习期不碰 validator、PoC 期也够；17:30 EVM 自动化轻量不冲突；test-validator 端口 8899/8900 现无人用。

### 🔴 今日主线 6：C 盘清理（06:18 → 06:29，安全策略严守）
- 用户：「先优化系统 C 盘，去掉些垃圾」。按 `personal_files_safety`：模糊高危请求 → **只读扫描列清单、禁自动删**；删前逐条确认 + ≤10/批 + 回收站非 rm + 先备份；禁递归清空系统/个人目录。
- 只读扫描结果：`C:\Windows\Temp` = 10 文件 / 0GB（可忽略）；`C:\Users\ADMINI~1\AppData\Local\Temp` = **10429 文件 / 0.811GB**（标准可删垃圾）。C 盘总剩 22.96GB → **装 Rust + Solana 已够，清理属可选优化**。
- 处置：**未删任何文件**。bulk 删 1 万文件受策略限制（需列全路径 + ≤10/批）→ 给两路：A) 我列 Temp top20 最大单文件供逐条确认删（回收站）；B) 用户自跑 Windows 磁盘清理（cleanmgr）/ 存储感知做系统级安全批量清理。
- **用户选 B（06:21）**，我未自动删任何文件。**清理结果（06:29 截图核）**：用户自跑 cleanmgr 完成 **753MB** 清理（Defender 防病毒 / 已下载程序文件 / Internet 临时文件 / 错误报告 / DirectX 着色器缓存 / 传递优化文件 200MB 等）；C 盘 **22.96 → 23.1 GB 可用（共 119 GB）**。判定 C 盘基本清完、到此为止；未推荐的深榨项：cleanmgr「其他选项」系统还原清理（会删回退点）、关闭休眠 `hiberfil.sys`（改系统行为，须用户明确同意）。

### 🔴 今日主线 7：清理 AI 自产临时 / 侦察文件（06:34 → 06:38）
- **已删（安全，非系统 / 非个人目录禁区）**：① `/c/tmp` 本会话 3 个临时（ram_check / env_check / cdisk_scan.txt）；② `/tmp` 28 个历史调试 `.log`（build / forge_build / mat-debug-* / tmx_poc* 等）；③ **`bounty-recon/chrome_session_cookies.json`（明文 session cookie，9/12 遗留，铁律① 安全隐患，已彻底 rm）**。剩 `/tmp/mat-debug-14076.log` 因进程占用（busy）未删，可忽略。
- **待确认**：`bounty-recon` 下 ~70 个侦察快照（`.json` / `.txt`：if_* / cantina_* / sherlock_* / det_* / immunefi_bounties*.json / nft_leads / options_nft_leads / github_fresh_* / practice_repos 等）**可安全批量删**（结论已进日志、脚本重抓即再生）；工具脚本（parse_*.py / *_probe.js / edge_probe.js / hyperbridge_withdraw_probe.js）建议留。
- **用户 06:38 决定「留着」**：~70 个侦察快照全部保留不删；工具脚本本就建议留。→ 清理收工：仅删了 `/tmp` 临时垃圾 + `chrome_session_cookies.json` 敏感文件，工作区维持现状。

### 🔴 今日主线 8：非 EVM 入门便携包（07:xx 用户外出授权「自行安全解决」）
- 产出 **`D:\non-evm-onboarding\README.md`**（便携包主文档）：Solana/Anchor 基线程序清单（5 个：spl-token / metadata / bubblegum / Anchor escrow / 真实新鲜靶 gmtrade·stackingdao·zest）+ Clarity 入门路径（Clarinet / clarity-book / stacking-dao / zest）+ **迁移版四透镜（EVM → Solana/Stacks 漏洞族映射表）** + 本机 Win / D 盘安装指南 + 纪律回扣（⑩⑪⑬⑭⑯ 不破 EVM 主线）。
- **关键事实校准**：① Anchor 当前官方文档 **Windows 必须走 WSL**（原生无 C++ 链接器 → `cargo install` 必失败）；② 本机 `wsl.exe` 存在但**被沙箱安全策略禁用系统级工具**，且 `cl/gcc/cc` 均无；③ 故**不擅自启 WSL / 重启**（会打断 EVM 17:30 自动化）。装法三选一已写入文档：A. WSL 一键（推荐）/ B. 原生仅 Rust + Solana CLI / C. MSVC 生成工具 + avm。
- 后台安装 `install_rust_solana.sh`（日志 `D:\non-evm-onboarding\install_rust_solana.log`）：仅 Rust（rustup → `D:\.rustup` / `D:\.cargo`）+ Solana CLI（预编译 → `D:\solana`），**Anchor 跳过**（记原因）。
- **⚠️ 安装失败根因（本次自动化运行 06:56 亲手复测定位）**：日志显示 `curl: (35) schannel: failed to receive handshake` 与 `curl: (23) client returned ERROR on write` → 复测证明**不是网络问题**：`curl` 从 `static.rust-lang.org` 下载 **200 / 12,721,664 bytes / 0.96s 到 C 盘临时目录完全成功**，但**同一 URL 写向 `/d/` 立即 `(23) client returned ERROR on write`、`size=0`** → **沙箱拦截 D 盘写入**。修复路径：先下到工作区 / 临时目录（可写），再由用户或非沙箱步骤落到 `D:\`；或该步申请沙箱外执行。**未擅自绕过**。
- 决策点留给用户：Anchor 编译 / 跑 PoC 须由其回后在安全中心开 WSL 策略，或装 VS2022 生成工具。**铁律：EVM 主线不变，非 EVM 空闲学。**

### 📌 真靶战线现状（承接 9/22）
- **Twyne #55649（Immunefi）→ 永久 dead**：9/21 spam 被拒 → 固化铁律⑭（影响主张须逐字回码对质）；9/22 ~16:08 的 48h 项目确认锚已过，**今日记忆将其归入「永久死」清单**，不再 ping 死活（铁律⑪ 口径见主线 3），仅作练手对象。
- **TermMax V2（Immunefi）→ PRIMARY 候选**：⑬ 同根因查重 PASS + ⑫ hunter 19 paid 可接受；报告九段式草稿已落盘（`bounty-recon/audit-notes/termmax-immunefi-report.md`，状态 LIVE / ready to submit）。**待办**：soldeer 依赖装完后编译真实 `TermMaxVaultV2` + `OrderManagerV2` 跑 mainnet-fork 集成 PoC + diff 部署字节码（屏蔽 immutable）→ 无撞车则主张走用户 Edge 提交（NoKYC）。
- **Midas #122 → 已结案**：#355 + #356 双 Duplicate，$40 押金全退、零亏损零奖金。**不再对该 program 投资本**。
- **TENOR-001（Sherlock #407）→ parked**：严重度只有 批判/高 两档且全需 250 USDC 存款，无 Medium 档 → 明确不值得投资。
- **Hinkal（HackenProof）→ 不提交**：四透镜深审无提交级候选 → EV=0（铁律⑩）。
- **orderly `contract-evm` / gammaswap v1-core → 各经四透镜 CLEAN，不提交**。
- **✗ 不为「有靶打」放宽过滤器**：Alchemix v3（EVM + 免费 + $150K，但上线 ≈204 天超 90 天红线）→ 不放宽。
- **#528 状态**：`in_progress`（监控态）。

### 🧪 练手资产（空档期沉淀，零 bounty EV、一律不提交）
- **盲卡库 38 张**：`total=38 / 冷审通过=38（全 4/4 PASS）/ Critical=15 / High=22 / Medium=1 / 覆盖族=38 / 结构化答案键=32`。面板 `bounty-recon/live-audit/blind-coverage-matrix.html`。
- **脏代码 spotting 训练样本**：`practice/alejo-vault/`（5 发现 + Foundry PoC 5/5 PASS + 五份合规报告练习，校准出「5 疑似里 1 真 bug」的双向偏置）/ `practice/nico-lending/`（无 High/Critical 可提交漏洞）/ `practice/blaze-phoenix/`（代码质量极高、零 Critical，不提交）。
- **其余靶**：`practice/siyam-perp/`（Critical 资金锁 4/4 PASS）/ `practice/maxwire-staking/`（诚实证伪对照 5/5 PASS）/ `practice/hinkal-revisit/` / `practice/starknet-l1-solidity/` / DeFi Saver V3（R1–R8 全 FALSIFIED，44 tests PASS）。
- **待令靶**：YieldNest Max Yield Vault（`practice/yieldnest-vault/`，audit-notes 骨架已写）。**未获「练」指令前不动，严禁提交任何 finding（铁律⑩）。**

### 🛠 工具 / 流程改进待办
- `cantina_diff.js` 的 ended 判定逻辑重写（按 card 内独立区块 / `href` 解析，替代整页关键词匹配）—— 连续两日误报，须修；**结论一律以 UUID 集合比对为准**。
- TermMax mainnet-fork PoC：soldeer 依赖安装完成后编译真实合约并与部署字节码 diff（屏蔽 immutable），证明「本地源码 == 主网代码」。
- 非 EVM 工具链安装：**改「先下到 C 盘可写目录 → 再落 D 盘」**（沙箱拦 D 盘写入，见主线 8）；Anchor 待用户开 WSL 或装 VS2022 生成工具。
- 自动化结果可见性：无 push 通道 + 部分 prompt 不落文件 → 结果静默；评估统一改为「落日志 + 用户开 app 时自读」或后续接通知通道。

### 🧭 铁律⑯ 指路（当下最该做且只做这件）
- 今日主线 1/2 已双重证否「有靶可打」→ **主线无立即提交动作**。
- 下一轮二选一（显式 defer 其余）：**① B（修侦察基建，中 ROI）**：修 `scan_github_fresh` 两缺陷（`max_results=40` 截断 + `stars>=3` 误杀）+ 接 Hats / HackenProof adapter → 提升下次靶出现时的命中率；**② 空窗静默等**（铁律⑪）：17:30 自动化档继续监控，不强行低 EV 提交。
- TermMax V2 仍为唯一「LIVE / ready to submit」候选，等 soldeer 依赖就绪再动。

---
*Day 70 · 空档第 32 天 · 四平台一手核 GOLD=0（真实空窗）· 非 EVM 评估与入门包就位（工具链安装待解 D 盘写入）*
