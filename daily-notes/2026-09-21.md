# 2026-09-21（Day 68）

## 今日状态

### 🎯 主战线：空档第 30 天（三源落笔时未出，Cantina 0 新赛）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：Immunefi 原始快照 `immunefi_bounties.json` 最后更新 **9/20 17:56**（今日 06:06 快照未落盘）→ **今日 07:30 金标流水线 / 09:00 三源侦察档产物（`scope-diff-2026-09-21.md` / `competitions-recon-2026-09-21.md`）落笔时均未生成**，待跑完后核对。
- **9/20 侦察基线**：三源全空窗（Immunefi 金标 0，`dead_filtered: mtpelerin / twyne / synthetix`；Sherlock 0；CodeHawks 0）。72h 新资产 79 全为既有 program 资产扩容（Aera / Spark 系列 / Pareto Credit vault），非新 bounty → 不产生新竞争窗口。
- **Cantina 每日侦察（06:40 运行）**：Edge 活体渲染 `cantina.xyz/opportunities` → **liveCount 51**。与 `cantina_recon_log.md` 中 9/19 建立的 51 条全量基线**逐 UUID（前 8 位）比对：`NEW=[]` / `GONE=[]`，100% 一致 → 0 新赛、0 下架**，按任务步骤④静默处理、不群发告警。产物 `cantina_diff_20260921.json` / `cantina_probe_20260921.json`。
  - ⚠️ **工具坑（9/20 已发现，今日再次绕过）**：`cantina_diff.js` 的 ended 判定启发式失效（把整页文本塞进每张 card → "Ended" 关键词全卡误命中）。**结论必须以 UUID 集合比对为准，不可信脚本的 liveCount/endedCount 字段**；今日已用基线前缀集合人工核过。脚本待重写（按 `href` 解析而非整页关键词匹配）。
- 空档纪律（铁律⑪）：练手仅在用户明确说「练」时开；否则静默监控金标，想干真活就深审 parked / 新筛的真靶。

### 🔴 今日主线 1：twyne #55649 状态活体核（用户 Edge 截图一手）
- **状态推进 ✅**：系统消息 *"Your report has been escalated directly to the project and Immunefi has been unsubscribed."* @ **2026-09-20 16:08**（提交 15:48 后 20 分钟）→ 流水线第二站 **Escalated to Twyne** 达成，triage / 反 spam 关已过。
- **✅ Submission 编号定案（05:17 顶部截图）**：正式编号 **#55649**（CoolPis69 / Whitehat for Twyne），徽章 **Critical + Escalated**，页面显示 *"The project is now aware of the bug report."*，钱包 **Verified Primary**。此前记忆中的 55648 系差 1 笔误（URL 里的 51849 / 93649 是内部路由 ID，非显示编号）。
- **时钟**：Critical 提交规则 = escalation 起 **48h 内项目确认收到**（→ **9/22 ~16:08**），**336h 内解决**（→ **~10/4 16:08**）。
- **纪律**：`unsubscribed` = 正常流程（转直连项目），**非被拒**；等待期勿重复提交同根因、勿催评论区。9/22 16:08 后若无确认动静 → 用 "ask for help" 让 Immunefi 重新介入（唯一该动的按钮）。

### 🔴 今日主线 2：acre 被一手核否 → 进功靶重选 orderlynetwork
- **acre 误判修正**：Immunefi explore RSC 全量 **174 个 slug 无 acre / stbtc** → 9/20 基于 WebSearch 的 acre 侦察不成立（**不在 Immunefi 公开 bounty**）。铁律①（情报须平台一手）应验：旧侦察 ≠ 一手。acre 移出候选。
- **一手筛 v2 甜区**：174 slug → 18 候选（NoKYC + ≤$100K + Solidity）→ 抓 5 个详情看 audits / hunter 词频。**orderlynetwork 最强**：audits=0 / hunter 词频最低（4） / EVM（5 个 etherscan 链接） / $100K / NoKYC / launch 2024-02（这批最新）。
- **orderlynetwork 资产确认（一手）**：开源 `github.com/OrderlyNetwork/contract-evm/tree/main/src` + `evm-cross-chain`；Arb / OP / Polygon 多合约；非 Solidity 面（api.orderly.org 等）按铁律⑩排除。项目方另有 Audits repo（外部审计），但 **Immunefi 登记 audits=0 仍算甜区**。
- **进功分工**：①我 = 克隆 `contract-evm` + 四透镜深审 + 双轴 PoC + ⑫活体核 hunter 真实数（Edge）；②用户 = ⑬登 Immunefi 开 orderlynetwork program 页手动搜同根因（自动化过不了 CF）。免费无押金（铁律⑩优先）。**#55649 等待期并行挂着不动**。

### 🔴 今日主线 3：orderly `contract-evm` 四透镜闭环 = CLEAN（不提交）
- 读完全量核心合约：`Vault` / `Ledger` + ImplA–D / `OperatorManager` + ImplA/B / `FeeManager` / `MarketManager` / `VaultManager` / `Signature` / `DelegateSwapSignature` / `SwapSignature` / `Utils` / `Ed25519` / zip 解压器。
- **结论**：**无非特权 submission-grade 漏洞 → 不提交**（falsification 证伪）。关键证据：
  - ① 所有资金面经 facade 修饰符门控（`onlyOwner` / `onlyOperator` / `onlyOperatorManager` / `onlyCrossChainManager` / `onlySwapOperator`）；
  - ② 用户提现 EIP-712 绑 `chainId` + `verifyingContract` + `withdrawNonce` → 重放锁死（`LedgerImplA` L122）；
  - ③ `delegateSwap` 由 `swapSigner` 签名 + `tradeId` 去重（`Vault` L799/841）；
  - ④ `Ed25519.sol` 标准实现，无 forge；
  - ⑤ `DecompressorExtension._decompressed` 内存安全，下游仍验 engine 签名。
- 剔除两处误判：**O-V1** `delegateSigner` 仅 emit 无状态；**O-V4** `operatorPing` 经 facade `onlyOperator` 门控。
- 观察项（**均非提交级**）：O-V2 `rebalanceMint` "Nonce already used"→success（可用性）/ O-V3 原生 deposit fee 信任 CCM / O-V5 `int128` funding 溢出批量 DoS / O-V6 settlement 余额 <0 检查被注释（提现仍硬挡）/ O-V7 `OperatorManagerZip.decompress` 无修饰符 delegatecall 原语（下游均门控）。
- `tUSDC.sol` = TestUSDC 测试币，非生产。审计笔记：`bounty-recon/audit-notes/orderly-2026-09-21.md`。

### 🔴 今日主线 4：gammaswap 四透镜闭环 = CLEAN（不提交）
- **靶**：`github.com/gammaswap`（$40K / NoKYC / 免费 / Solidity / launch 2023-09 / Immunefi Last Updated 2026-05-10 活跃）；已克隆 `v1-core`(128 .sol) / `v1-periphery` / `v1-implementations`。
- **深读**：v1-core 全策略层 + `BaseStrategy`/`BaseLong`/`BaseRebalance`/`BaseBorrow`/`BaseRepay`/`BaseLiquidation` + `External*`/Single/Batch liquidation + `GammaSwapLibrary`/`GSMath` + observer（`AbstractLoanObserver`）+ `PositionManager`（v1-periphery 路由层）；另扫 v1-implementations 具体 CFMM 策略（CPMM/DSV2）确认无裸 external 资金入口。
- **🔑 关键证伪（推翻早间重入假设）**：`GammaPool` dispatcher 虽 `whenNotPaused(N)` 无 `lock`，但所有资金入口经 `callStrategy = strategy.delegatecall` 落到 **leaf 全带 `lock`**（`_borrowLiquidity` / `_repayLiquidity` / `_repayLiquiditySetRatio` / `_repayLiquidityWithLP` / `_rebalanceCollateral` / `_updatePool` / `_liquidate` / `_liquidateWithLP` / `_batchLiquidations` / `_rebalanceExternally` / `_liquidateExternally` / `_deposit` / `_mint` / `_withdraw` / `_redeem` / `createLoan` 均 `lock`）。`lock`（`AppStorage.sol:19`）设共享 `s.unlocked`，delegatecall 同域 → 回调期间（`IExternalCallee(to).externalCall`）重入任意入口一律 revert `Locked()`。**重入面被全挡，lens①/④ 高危假设否**。
- **G-1 证伪**：`clearToken` 仅拦 CFMM-LP + 抵押 token；池自身 GS（`address(this)`）可清但无底层价值（仅自身份额记账）→ 良性，非抽干。
- **G-2 维持**：`factory.execute` 仅 `feeToSetter`（owner 级）可信，非提交级。
- **记账稳健（lens②）**：借出 `convertLPToInvariantRoundUp`（向上取整，利池）；repay `paidLiquidity = min(loanLiquidity, 实际余额增量)` 治 transfer-fee；`updateCollateral` / `checkLPTokens` 读真实余额强校验 → 无「借多还少 / 稀释抽干」舍入漏洞。
- **权限（lens③）**：loan NFT `isAuthorizedForToken` + pool `_getLoan` 绑创建者；observer 注入受 `setLoanObserver` 接口校验 + `FORBIDDEN` 限制，用户不可自注册；UUPS `_authorizeUpgrade` 仅 `onlyOwner`、`TwoStepOwnable`。
- **结论**：核心 + 路由层 **CLEAN → 不提交**。审计笔记 `bounty-recon/gammaswap/audit-notes/gammaswap-core-2026-09-20.md`。
- **竞争判读**：公开 *Attack Vectors & Countermeasures* 文档 + 2023-10 Transfer Fees Attack 真实攻击史（已重部署修复）→ 竞争者认知度高、代码已夯实，**竞争 HIGH、EV 偏低**，故按 falsification 主动收手。

### 🟢 今日主线 5：TermMax V2 四透镜 → PRIMARY 候选（待用户 ⑬/⑫ 后定提交）
- **定靶（续接深审）**：一手 `immunefi_bounties.json` 过 v2 闸 → **TermMax**（$80K / NoKYC / EVM / audits=1「ABDK 仅审 v1 代」）胜出。克隆 `termax-contract-v2`（v1 = 老代已审，**v2 = 线上未审 → 本审对象**）。
- **四透镜执行**：code-explorer 出 `contracts/v2/` 16 核心合约结构图（14 red flags）→ 逐字读 `OrderManagerV2`(341L) / `TermMaxVaultV2`(741L) / `DelegateAble`(111L) 验证。
- **🔴 PRIMARY 发现 —— 坏账非原子结算 → 份额价通胀 → 储户资金被盗**（透镜②经济/会计 + 透镜③信任）：
  - `OrderManagerV2.redeemOrder`（`vault/OrderManagerV2.sol:89-108`）记 `_badDebtMapping[collateral] += badDebt` 且把真实资产存回池，**但不减 `_accretingPrincipal` / `_totalFt`** ← 根因。
  - `TermMaxVaultV2.totalAssets()`（`:334`）= `_previewAccruedInterest().previewPrincipal / DECIMAL_BASE_SQ`，**纯内部账、从不读真实余额** → 坏账窗口内仍报满额 = 份额价通胀。
  - `redeem` / `withdraw` / `dealBadDebt` 均**无 `whenNotPaused`**（`:360 _withdraw` 无 pause，`:342 _deposit` 有）且按通胀 `totalAssets()` 计价；`dealBadDebt`（`:409`）**无角色门控**（任何份额持有人可触发核销）。
  - **攻击链**：市场违约 → curator `redeemOrder(坏账)` → 储户 A 抢跑 `redeem` 按通胀价多抽；之后 `dealBadDebt` 减记 → 储户 B 赎回 revert，承担全部坏账。**非特权储户盗取同池用户本金**。
  - **PoC** `bounty-recon/termmax/poc/` 逐字复刻源算术，`forge test` **PASS(1 passed)**：A 存 500 / B 存 500 → 坏账 300 → A 赎回得 500（> 公平 350）→ B 赎回 revert。
  - **严重度主张 Critical**：program 影响分类把 `Direct theft of any user funds … other than unclaimed yield` 列为 Critical，`Theft of unclaimed yield` 才是 High；本 finding 盗的是**储户本金**（B 的本金经通胀份额价转给 A），落在 Critical 桶。奖励 = 受影响资金 10%，program max **$80,000**（明细文本引用 $50K；`programRewards.maxReward=80000`），min $2,000。**注意 caveat**：vault 可暂停 → 触发 Immunefi「可暂停/可升级合约仅首次攻击计奖」条款（首次仍是直接盗本金 → Critical 成立，只是不可累计多窗口）。
- **🟡 次级假设**：H1 `afterSwap` 近端到期利息通胀（`deltaAnnualizedInterest = ftChanges*365d/(maturity-now)`，`maturity-now→0` 除小；`_checkApy` 仅 borrow 路径）—— 多为 by-design（`totalAssets()` 仅计截至 now 累计利息）。H2 `_withdraw` 缺 pause（强化 primary）。
- **🟢 已证伪排除**：① Router delegatecall-to-adapter（须 `WHITELIST_ROLE`，特权信任，非非特权可利用）② `afterSwap` 直调（`_orderMaturityMapping[msg.sender] != 0` 挡，攻击者非注册 order）③ EIP-712 `setDelegateWithSignature`（nonce + deadline + `ecrecover == delegator` + OZ `_domainSeparatorV4` 含 chainid/verifyingContract，重放锁死）④ `MakerHelper.mint/burn` 缺 `onlyWhitelisted`（仅拉 own debt token，无第三方资金面，低危）。
- **✅ ⑬ 同根因查重 PASS（2026-09-21）**：Edge 开 `termstructurelabs` program 页 + 英文 RSC payload 核验 → info/scope 中 **bad debt / share price / redeem / dealBadDebt / ERC4626 / accounting 出现 0 次**；Known Issues 仅 v1 ABDK 报告。**⑫ hunter 密度可接受**（19 paid reports / 中等，非红海）。
- **范围确认**：program 覆盖 **"Term Structure Labs Codebase"（Program Codebase）整库**，含 V2 Vault / OrderManager；显式列出的资金资产仅 TMX token（ETH `0x3c2f…0039` + BNB）。
- **落盘**：审计笔记 `bounty-recon/audit-notes/termmax-2026-09-21.md`；Immunefi 报告草稿 `bounty-recon/audit-notes/termmax-immunefi-report.md`（九段式，状态 `LIVE / ready to submit`）。
- **待办（提交前门控）**：依赖就绪 → 编译真实 `TermMaxVaultV2` + `OrderManagerV2` 跑集成 PoC + diff 部署字节码（屏蔽 immutable；soldeer 装依赖后台跑中）→ 无撞车则 **Critical 主张走用户 Edge 提交（NoKYC）**。⚠️ **勿与 2026-08-23 Term Labs ~$8.5M 治理攻击混淆**（那是旧 Term Meta Vaults，非本 vault bug）。
- **纪律修正**：MEMORY.md 死路表中原「TermMax（红海）」条目已移除 —— 红海系误判翻案（ABDK 只审 v1 代，v2 线上未覆盖 = 甜区靶）。

### 📌 真靶战线现状（承接 9/20）
- **twyne #55649（Immunefi，Critical）→ 等待期**：Escalated（9/20 16:08），48h 确认锚 **9/22 ~16:08**，336h 解决锚 **~10/4 16:08**。等待期纪律：不重复提交、不催评论。
- **TermMax（Immunefi）→ PRIMARY 候选**：⑬/⑫ 已过，报告已起草，待 mainnet-fork PoC 升级 + 用户 Edge 提交。
- **Midas #122 → 已结案**：#355 + #356 双 Duplicate，$40 押金全退、零亏损、零奖金。**不再对该 program 投资本**。
- **TENOR-001（Sherlock #407）→ parked**：提交流严重度只有 批判/高 两档且全需 250 USDC 存款，**无 Medium 档**；Tenor 官方安全页明示 bounty 只跑 Sherlock、无免费镜像。技术上仍有效，但**明确不值得为此投资**。
- **Hinkal（HackenProof）→ 不提交**：四透镜深审无提交级候选 → EV=0，按铁律⑩不提交（省 amlbot KYC 触发与精力）。
- **orderly / gammaswap → 今日各经四透镜 CLEAN，不提交**（见主线 3、4）。
- **✗ 不为「有靶打」放宽过滤器**：Alchemix v3（EVM + 免费 + $150K，但上线 ≈204 天超 90 天红线）→ 不放宽。

### 🧪 练手资产（空档期沉淀，零 bounty EV、一律不提交）
- **盲卡库 38 张**：`total=38 / 冷审通过=38（全 4/4 PASS）/ Critical=15 / High=22 / Medium=1 / 覆盖族=38 / 结构化答案键=32`。面板 `bounty-recon/live-audit/blind-coverage-matrix.html`。
- **脏代码 spotting 训练样本**：`practice/alejo-vault/`（5 发现 + Foundry PoC 5/5 PASS + 五份合规报告练习，校准出「5 疑似里 1 真 bug」的双向偏置）/ `practice/nico-lending/`（无 High/Critical 可提交漏洞）/ `practice/blaze-phoenix/`（代码质量极高、零 Critical，不提交）。
- **其余靶**：`practice/siyam-perp/`（Critical 资金锁 4/4 PASS）/ `practice/maxwire-staking/`（诚实证伪对照 5/5 PASS）/ `practice/hinkal-revisit/` / `practice/starknet-l1-solidity/` / DeFi Saver V3（R1–R8 全 FALSIFIED，44 tests PASS）。
- **待令靶**：YieldNest Max Yield Vault（`practice/yieldnest-vault/`，audit-notes 骨架已写）。**未获「练」指令前不动，严禁提交任何 finding（铁律⑩）。**

### 🛠 工具 / 流程改进待办
- `cantina_diff.js` 的 ended 判定逻辑重写（按 card 内独立区块 / `href` 解析，替代整页关键词匹配）—— 连续两日误报，须修。
- TermMax mainnet-fork PoC：soldeer 依赖安装完成后编译真实 `TermMaxVaultV2` + `OrderManagerV2`，并与部署字节码 diff（屏蔽 immutable）以证明「本地源码 == 主网代码」。
