# 2026-09-24（Day 71）

## 今日状态

### 🎯 主战线：空档第 33 天（今日 04:0x 手动跑金标流水线交叉印证 → GOLD=0 为真）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:55）**：今日 07:30 金标流水线（`automation-1788146085830`）/ 09:00 三源侦察档产物（`scope-diff-2026-09-24.md` / `competitions-recon-2026-09-24.md`）均**未生成**，待跑完后核对。今日空窗结论**不依赖**它们 —— 来自 04:0x 手动分源跑 + 昨日四平台一手活体核。

### 🔴 今日主线 1：手动跑 gold_pipeline（04:0x，分源独立进程规避沙箱 kill）
- **背景**：完整多源长扫在沙箱内被进程组 kill → 改按「单源独立 Bash 调用」逐源跑（每源独立进程组，单源 kill 不蔓延）。
- **Immunefi scan_gold（P0 唯一可提交面）**：`fetch_failed=False`、`cache_age_h=0.0`（数据新鲜可信）、拉取 235 条赏金 → **gold=0 / killed=0 / fallback=0 / dead_filtered=3**（mtpelerin / twyne / synthetix，已知死路排除）。→ 空窗确为真，非脚本缺陷。
- **P2 情报**：Sherlock=0、CodeHawks=0。
- **扩面 leads**：github_fresh=400（上游新鲜仓，多数 0–2★，defisaver-v3 378★；**练手池非提交面**）；HackenProof=12（全 kyc=True 违 NoKYC，含 Starknet/NEAR 非 EVM，4 个命中死路已 skip）；whitelist=29（8 fresh，Uniswap/Morpho 蓝筹新模块，红海曝光）。
- **leads→GOLD / whitelist→GOLD 匹配**：活跃池 Immunefi/Sherlock 均 0 → 无 promote，不升金标。
- **结论**：可提交金标 = 0，空窗连续（自 8/31）。与铁律⑪/⑯ 一致：空窗静默监控，不深审不提交。07:30 自动化将用修好的可信数据（昨修 `gold_pipeline` Immunefi 原子 fetch）正常产出。

### 🔴 今日主线 2：StackingDAO（Clarity/Stacks）四透镜练手**全闭环**（12 逻辑层 / 11 笔记，全无提交级）
- 仓库 `StackingDAO/contracts`@main tarball 克隆至 `D:\non-evm-onboarding\stackingdao-contracts`（170 .clar；C 盘下载→D 盘解包，规避沙箱拦截 `/d/` 直写）。
- `clarinet check` 29 errors / 537 warnings：**零类型/语法错误**，全为 `pox-4`(26) 与 `data-core-v1`(3) 主网/测试网固定地址本地不可解析的环境产物 → 无法本地跑集成测试，以静态四透镜 + 逐字回码对质为主，**不做 PoC**。
- **本轮新增深读的 7 层**（承接昨日 5 层）：
  - `stacking-pool-v1.clar`(543L)：H-1/H-2（`delegate-stx` L108 / `revoke-delegate-stx` L119 公开无 `check-is-protocol` gate，与同文件 L346-425 全 gated 不一致）**经 STX 托管链追踪降级为信息级** —— STX 不在池子而在 `.stacking-delegate-1-X`，pool 只是 delegate、自身≈0 余额，撤销自身委托不破坏 delegate stacking。笔记 `audit-notes-stacking-pool-v1.md`。
  - `native-stacking-pool-v1.clar`(388L)：权限模型良（全 `check-caller-allowed` 门控，`allow-contract-caller` 仅 `tx-sender==contract-caller` 可改防自授）。**N-1 信息级会计 bug**：`map-set-details` 无条件 `grouped-totals += lock-amount`，extend-only 分支返回全额致重跑批次重复计；但 grep 证 `grouped-totals` 仅被只读 getter 消费、**无任何转账/授权路径消费** → 纯链下信息、触发者=运营方(门控)、无链上资损 → 非提交级。笔记 `audit-notes-native-stacking-pool-v1.md`。
  - `rewards-v4.clar`(331L) + `rewards-v5.clar`(385L)：`dao.clar:185` 注册 `.rewards-v5` active → v5 为当前激活。v5 收紧（staking 地址钉死 `ERR_WRONG_STAKING`、interval 整除校验、sBTC v1/v2 supply 拆分+除零保护，修复 v4 的 O-2）。R-1~R-4 全信息级/版本说明。笔记 `audit-notes-rewards-v4-v5.md`。
  - `strategy-v3` 家族（`strategy-v3` 276L + `-pools-v1` 141L + `-delegates-v1` 100L + `-algo-v1` 240L + 下游 `delegates-handler-v1` 291L）：四步 prepare-pools→prepare-delegates→execute→return-unlocked-stx；`execute` 委托额取自内部 `prepare-delegates-data`（**非调用方参数**，防金额注入）；`compare-delegates` 强制顺序严格匹配。**S-3 闭合为安全**：`return-unlocked-stx`(无 gate) 下游 `handle-excess`/`revoke-*` 强制 `check-is-protocol` → 恶意 trait 注入被拒。S-1/S-2/S-4/S-5 皆信息级（含 `lowest-combination` 仅枚举连续子集、非全局最小，命名暗示≠实现）。笔记 `audit-notes-strategy-v3.md`。
  - `keeper-jobs/` 三件套（`rewards-job-v1` 63L / `return-stx-job-v1` 53L / `tax-v1` 154L）：均 `(impl-trait arkadiko-automation-trait-v1)` → `run-job` public 是 Arkadiko keeper 网络自动触发约定（设计意图）。R-J / RS-J 经追踪判 CLEAN（金额=自身余额+时序+幂等闸门；金额/目的地全协议内部化）。**唯一真阳性 T-J-1（tax-v1，lens ② 经济观察 Low–Med）**：`run-job` public → 任意人可触发 `handle-tax`；L86-92 `swap-y-for-x ... u1`（`min-out` 硬编码 1 micro-STX）→ 近零滑点保护，攻击者可 sandwich/抢跑致协议价值被 MEV 抽走（不直接窃币但价值泄漏）；L85/L95 `TODO: update for mainnet` 提示非生产终态。笔记 `audit-notes-keeper-jobs.md`。
  - `data-direct-stacking-v1.clar`(144L)：grep 下游消费 = 仅 `direct-helpers-v1..v4` + `strategy-v3-pools-v1`（全协议内部）。**一致性验证**：`direct-helpers-v4` 增/减/删路径 read-then-write `(+/- delta)` 配对，user/pool/total 三者同幅变动 → 账本内部自洽，**排除 native 层那种累加重复计 bug**。两 setter 皆 `check-is-protocol` 门控、只喂 strategy 分配 → **CLEAN**。笔记 `audit-notes-data-direct-stacking-v1.md`。
  - `ststxbtc-tracking.clar`(200L)=**v1**：**存活确认（一手证据）**：`dao.clar:174` active 注册 + `Clarinet.toml:103` 部署 + 生产调用方 `rewards-v3/4/5`(L217/237/271) 与 `ststxbtc-token` 转账路径 `refresh-wallet`(5 处)。**v1 与 v2 并行双账本**（rewards-v5 同时 L271 喂 v1、L275 喂 v2）。`add-rewards`/`refresh-position`/`claim-pending-rewards` 无 gate 但安全-by-design（调用方付费、须在白名单、payout 恒到 holder 非 caller）→ 无提交级。笔记 `audit-notes-ststxbtc-tracking-v1.md`。
  - 二级 NFT 市场（7 合约共用 `buy-in-ustx`/`list-in-ustx`/`unlist-in-ustx`，代表 `ststx-withdraw-nft-v2.clar` 220L）：**核心安全属性全成立** —— `is-sender-owner` 双重校验（`tx-sender==owner && contract-caller==owner`）防冒名上架；`transfer` 上架 NFT 禁转防双重卖；`buy-in-ustx` 先付款后转 NFT + 原子回滚 + commission 调包防护（地址绑定）。两条信息级：NF-1 commission 不透明且不由 protocol 白名单约束（卖家可指定任意地址，地址公开可审查→设计特性）；NF-2 commission trait 版本分叉（`.commission-trait` 仅 tests mock 实现，生产用 `.commission-trait-v1` → 生产接线待一手部署确认）。笔记 `audit-notes-nft-marketplace.md`。
  - 主池 vs signer-v1 权限分叉：**环境限制** tarball 克隆无 `.git` → git history 不可用，改用代码职责对比。事实：main-pool 8 处敏感操作全 `check-is-protocol`(L295-417)；signer-v1 全 `pool-owner` 单 owner 模型。**分叉=职责驱动有意设计**（main-pool=协议聚合层；signer-v1=keeper 直驱叶子，keeper=owner）。**SP-1 信息级**：signer-v1 `pool-owner` 单点信任 → 泄露可注入恶意 signer → PoX 验签失败 → aggregation 被拒（griefing/DoS，非窃资）。笔记 `audit-notes-signer-vs-mainpool-permission-split.md`。
- **总索引 `audit-index-stackingdao.md` 升级至 12 逻辑层 / 11 笔记**（覆盖率矩阵 + 七条纪律印证表含 D5 真阳性识别 + 信息级速查含 NF-1/NF-2/SP-1 + residual **已清空**）。
- **结论：无提交级 finding**（铁律⑩ 非 EVM 练手不提交）。核心纪律印证：①public+能转 STX≠可窃资（须追托管）②看着没验签≠缺失验证（须查被调方一手定义）③内部计数器重复加≠会计漏洞（须 grep 是否被资金路径消费）④第一直觉 Critical 模式须回码对质 ⑤入口 public 无 gate 须追下游闸门 + 金额源自内部 state 非调用方。

### 🔴 今日主线 3：固化非 EVM 审计通用模板 SOP skill
- **新建 user-level 伞形 SOP `non-evm-clarity-audit-sop`**（`~/.workbuddy/skills/non-evm-clarity-audit-sop/SKILL.md`）：把 StackingDAO 12 层练手全流程沉淀为通用模板 —— ① 本地环境（Clarinet 绝对路径 / 沙箱 `curl -o /d/` 写盘封锁须先落 C 盘 temp 再 tar 到 D / GBK 吞输出）② tarball 克隆纪律（无 `.git`→不能查 history，改用一手部署/调用证据）③ `clarinet check` 29 errors 全为 pox-4/data-core-v1 地址本地不可解析（零语法错→静态推理）④ 迁移版四透镜 Clarity 映射 ⑤ 笔记+总索引模板 ⑥ 引用子纪律 `non-evm-gateless-falsification`。
- **子 skill `non-evm-gateless-falsification` 同步加交叉引用**指向伞形 SOP（避免 skill 孤岛）。至此非 EVM 练手方法论双 skill 成形。

### 🔴 今日主线 4：每日金标监控自动化上线 + Discord 外部推送端到端打通
- **新建 automation `每日金标监控 (gold_pipeline / Immunefi)`**（id `automation-1790201448486`，ACTIVE）：只跑 Immunefi 单源（`immunefi_scan.py --fetch --xcheck-gold-only --json`，托管 python 全路径），规避完整 gold_pipeline 沙箱多源长扫 kill。行为：GOLD 非空→立即列出 slug/资产/maxBounty/审计数/上线时间并告警（排除 dead_filtered）；GOLD 空→仅一行状态、不克隆不深审；`immunefi_fetch_failed`/`cache_age_h>48` 大声报错。目的：解决铁律⑪「漏掉开赛瞬间」风险。
- **频率升级**：用户要求「立刻知道」→ `FREQ=DAILY` → **`FREQ=HOURLY`**（延迟 ≤24h 压到 ≤1h）。
- **新增 `bounty-recon/alert_push.py`**（仅 stdlib，免 venv）：从 `.alert_config.json` 读 webhook 配置，支持 discord(url) / telegram(token+chat_id) / generic(url)；POST 走 urllib，可选 `proxy` 字段或 `HTTPS_PROXY`。自动化 prompt 第 5 步：GOLD 非空或抓取失败 → 写 `gold_alert.txt` → 调 `alert_push.py --file`；**推送失败则应用内完整兜底输出，绝不丢告警**。
- **端到端打通**：用户在 Discord 自建私有服务器「金标告警」(server id `1352449565888578988`) 建 webhook → 填 `.alert_config.json`。
- **关键坑修复（可复用）**：首测 **PUSH_FAILED HTTP 403**；诊断 GET webhook=200（有效）、出网正常 → 根因 = **Discord/Cloudflare 拒绝 `Python-urllib` 默认 User-Agent**（GET 能过、POST 被 403）。修复：`alert_push.py post()` 加浏览器 `User-Agent` → 重测 **204 成功**。**教训**：任何用 urllib 直推 Discord/Telegram webhook 的脚本必须带浏览器 UA，否则 POST 403；此坑与代理/网络无关（直连与 127.0.0.1:7897 代理下均 403，加 UA 后均 204）。

### 🔴 今日主线 5：非 EVM 转赛道评估（用户 5mo 0 收入、亏 100+U，考虑 pivot Solana / Stacks）
- 用户诉求：评估转非 EVM（Rust / Clarity）审计可行性 + 学习成本 + 首笔收入时间线。
- **市场事实（实锤）**：Immunefi 新鲜 NoKYC ≤$100K 段 **EVM=0**，但 Solana / Stacks 有 `gmtrade`(78d/Solana)、`zest-protocol-v2`(249d/Stacks)、`stackingdao`(Stacks) 活靶；且用户 Immunefi KYC 已验证(9/19) → **KYC 类 Immunefi 靶也能打** → 非 EVM 机会面比「新鲜 NoKYC 小池」更大。
- **关键诚实点**：用户 EVM 5mo 0 收入的瓶颈是「**真实协议应用层**」（非语言），转 Solana 会原样搬此瓶颈 + **再加语言/运行时门槛** → 不能指望换语言就快赚钱。
- **学习成本**：**Clarity 极低**（2–3 周能审）但盘子小、奖金低；**Rust + Solana/Anchor 中高**（6–10 周能审计）。
- **时间线（假设每日 2–4h）**：Clarity 首笔 **2–4mo** 但金额小（<$1K）；Solana 首笔保守 **4–6mo**（撞上新鲜未审程序可缩至 3mo）。
- **推荐混合路径（最低机会成本）**：EVM 自动化监控继续白嫖（0 成本）；并行学 Solana/Anchor + 顺手 Clarity；等新鲜低竞争 Solana 程序出现再重兵压上；**别全切**（EVM 仍是总奖金最大盘，5mo 代码阅读能力有复利）。
- **机器 RAM 实测**：物理内存 **15.94 GB** → 修正「16G+」红旗为**琥珀级**：`solana-test-validator`（审计 PoC 用）8–16G 即可跑小程序；全节点 mainnet validator 需 256G+ 与审计无关。真实空闲约 11–12GB，跑前清 Chrome 残留 + `--limit-ledger-size` 即可。**机器非障碍**。

### 🔴 今日主线 6：换机 vs 本机学（06:04 → 06:11 用户改主意）
- 06:04 问「换另一台电脑学非 EVM 行不」→ **行**：学习期对机器零硬要求，前两周纯读代码甚至不需装工具链。可移植资产 = `~/.workbuddy/skills/` 的 audit-* 方法论系列（最值钱）+ MEMORY.md / 每日日志 + `audit-notes/` + `bounty-recon/` 脚本；EVM 17:30 自动化留本机继续跑。红线：私钥 / seed 绝不移机；另一台登录用 Edge（铁律⑨）。
- **06:11 用户改主意：在本机学**。环境实测：**C 盘剩 22.96 GB**（够装 Rust + Solana + Anchor 3–8GB）；`rustc` / `solana` / `anchor` **均未装**（干净安装无冲突）。→ 本机学无实质冲突：装工具尊重 D 盘惯例（`CARGO_HOME` / `RUSTUP_HOME` / Solana 装 D），RAM 15.94G 学习期不碰 validator、PoC 期也够；17:30 EVM 自动化轻量不冲突；test-validator 端口 8899/8900 现无人用。

### 🔴 今日主线 7：C 盘清理 + AI 自产临时文件清理（严守 `personal_files_safety`）
- **C 盘清理（06:18 → 06:29）**：用户「先优化系统 C 盘，去掉些垃圾」→ 按安全策略**只读扫描列清单、禁自动删**。扫描：`C:\Windows\Temp` 10 文件/0GB；`AppData\Local\Temp` **10429 文件 / 0.811GB**（标准可删垃圾）。bulk 删 1 万文件受策略限制（需列全路径 + ≤10/批）→ 给两路，**用户选自跑 Windows 磁盘清理（cleanmgr）**，我未自动删任何文件。**清理结果（06:29 截图核）**：cleanmgr 完成 **753MB** 清理；C 盘 **22.96 → 23.1 GB 可用（共 119 GB）**。未推荐的深榨项：cleanmgr「其他选项」系统还原清理（会删回退点）、关闭休眠 `hiberfil.sys`（改系统行为，须用户明确同意）。
- **AI 自产临时/侦察文件清理（06:34 → 06:38）**：**已删**（安全，非系统/非个人目录禁区）① `/c/tmp` 本会话 3 个临时；② `/tmp` 28 个历史调试 `.log`；③ **`bounty-recon/chrome_session_cookies.json`（明文 session cookie，9/12 遗留，安全隐患，已彻底 rm）**。剩 `/tmp/mat-debug-14076.log` 因进程占用未删，可忽略。**待确认**：`bounty-recon` 下 ~70 个侦察快照（`.json`/`.txt`）可安全批量删 → **用户 06:38 决定「留着」**，全部保留不删；工具脚本本就建议留。→ 清理收工。

### 🔴 今日主线 8：非 EVM 入门便携包（07:xx 用户外出授权「自行安全解决」）
- 产出 **`D:\non-evm-onboarding\README.md`**（便携包主文档）：Solana/Anchor 基线程序清单（5 个：spl-token / metadata / bubblegum / Anchor escrow / 真实新鲜靶 gmtrade·stackingdao·zest）+ Clarity 入门路径（Clarinet / clarity-book / stacking-dao / zest）+ **迁移版四透镜（EVM → Solana/Stacks 漏洞族映射表）** + 本机 Win / D 盘安装指南 + 纪律回扣（⑩⑪⑬⑭⑯ 不破 EVM 主线）。
- **关键事实校准**：① Anchor 当前官方文档 **Windows 必须走 WSL**（原生无 C++ 链接器 → `cargo install` 必失败）；② 本机 `wsl.exe` 存在但**被沙箱安全策略禁用系统级工具**，且 `cl/gcc/cc` 均无；③ 故**不擅自启 WSL / 重启**（会打断 EVM 17:30 自动化）。装法三选一已写入文档：A. WSL 一键（推荐）/ B. 原生仅 Rust + Solana CLI / C. MSVC 生成工具 + avm。

### 📌 承接与待办
- **金标空窗连续自 8/31（今日第 33 天）**：铁律⑪ 维持静默监控；新 automation `1790201448486` HOURLY 跑 Immunefi 单源 + Discord 告警。
- **非 EVM 路径**：用户 06:11 定「本机学」；下一步 = 按 `D:\non-evm-onboarding\README.md` 三选一装工具链（装 D 盘、先落 C 盘再移）。
- **真靶**：TermMax V2 仍唯一 LIVE/ready（等 soldeer + mainnet-fork PoC + 用户 Edge 提交）；Twyne 转永久 dead；Midas 结案 / Tenor parked / Hinkal 不提交；orderly·gammaswap 四透镜 CLEAN。
- **环境限制记录**：沙箱内跑完整 gold_pipeline 会被进程组 kill（分源跑规避）；WSL 被安全策略禁用（须用户在安全中心开启）。
