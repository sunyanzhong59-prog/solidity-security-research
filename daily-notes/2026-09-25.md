# 2026-09-25（Day 72）

## 今日状态

### 🎯 主战线：空档第 34 天（金标连续空窗自 8/31）
- **选靶硬标准 v2 现行**：上线窗口 <90d + 真实 GitHub 审计 ≤1（`--xcheck` 交叉核对，>1 红海毙）+ 公开 GitHub 源码 + No KYC + 奖金 ≤$100K。仅 Immunefi 为免费金标主源。
- **今日例行进度（落笔时 06:56）**：今日 07:30 金标流水线 / 09:00 三源侦察档产物（`scope-diff-2026-09-25.md` / `competitions-recon-2026-09-25.md`）**均未生成**，待跑完后核对。今日结论**不依赖**它们 —— 来自今日 04:0x–06:0x 的一手侦察 + 四透镜深审。
- **新增监控基建**：`每日金标监控 (gold_pipeline / Immunefi)` automation（id `automation-1790201448486`，`FREQ=HOURLY`）已上线，单源跑 `immunefi_scan.py` + Discord webhook 告警（`bounty-recon/alert_push.py`），把「漏掉开赛瞬间」风险从 ≤24h 压到 ≤1h。

### 🔴 今日主线 1：Tenor #407 旧笔记回访 → 结论「无需重做」
- **触发**：用户问「clone `tenor-labs/tenor-contracts` 再搭 Foundry 跑四透镜还有必要吗」。
- **结论：无需重做**。项目记忆里「Tenor #407（parked 锁死）」实为**已完审项**，非未审/新靶。
- **已有完整工程** `bounty-recon/practice/tenor-labs/`：`src`(router/bundler/gates) + 多个 Foundry 测试 + Certora FV 报告 PDF；`AUDIT_REPORT.md`(2026-09-15 四透镜深审) + `SUBMISSION_DRAFT.md` + `SUBMIT_CHECKLIST.md` 全齐；顶层 `tenor_407_probe.txt` / `probe_tenor_407.js` 为 #407 一手侦察。
- **真实新发现 TENOR-001（Medium）**：`VaultV2AllowlistGate.canSendAssets` 漏 fee-recipient 豁免（四门面 3 豁免 1 不豁免；同源 `canSendShares` 卡死已被 Guardian L-06 修，`canSendAssets` 被遗忘）。非特权、确定性、真实资金卡死/费用链路断裂；已过 8 份历史审计去重 + 十问自核 + #407 Known Issues 核（0 命中同款）。
- **锁死真因（非审计缺口）**：① Sherlock bug bounty 押金按 program 配，旧「零押金」是错（复刻 Midas #122 错）；#407 可能押 $250，钱包仅 41.62 USDC 不够；② #407 无 Medium 奖档（仅 Critical/High），TENOR-001 自定 Medium 错配，triager 可能定 High→触发押金 或 定低于 High→无奖档；③ KYC 发奖需终身 ≥2 有效 issue + 20% 命中，本 issue 仅 1，单独领不到。
- **行动**：勿重 clone；若真推进提交，杠杆在「给钱包注资覆盖押金 + 接受仅攒 1/2 有效 issue 战绩」，属花钱决策（铁律③⑥），需主人拍板；9/24 已定「别推花钱」。已回填 MEMORY.md 已结案段注明锁死真因。

### 🔴 今日主线 2：lending-protocol-engine C2 收口（B 选项短名单第 1 件）
- **逐字回码对质（铁律⑰）**：grep 确认 `LendingPool.sol` 全部外部调用函数 **0 命中 `nonReentrant`**；CEI 顺序核 `borrow`(L349-352 先增债后 safeTransfer) / `liquidate`(L416-419 先减债减份额后 `vault.redeem` L423) / `depositCollateral`(L248-255 状态在 safeTransferFrom + vault.deposit 之后) → 标准 ERC20/4626 无重入抽干。
- **机验（自包含 PoC `poc-c1-donation-inflation/test/PocC2.t.sol`，3/3 PASS）**：新增 `ReentrantERC20Lite`（ERC777 风格 transferFrom hook）+ 改 `ERC20Lite` 加 `virtual`：
  - ✅ `test_c2_control_unarmedNormalFlow`：hook 未武装=标准 ERC20，deposit + borrow(cap) + withdraw 正常（基线）。
  - ✅ `test_c2_reenterBorrowReverts`：hook 在 `depositCollateral` 的 safeTransferFrom 期（状态未增）调 borrow → `collateralSharesOf[msg.sender==token]==0` revert → 整笔 deposit 回滚 → **重入借出不可行被证伪**。
  - ✅ `test_c2_reenterDepositBenign`：hook 调 depositCollateral(Y)（记到 token 名下）→ 攻击者自身账目仍精确 = X，无污染 → 重入可达但良性。
- **结论**：C2 = **Info/Low 防御性**（依赖资产不可重入假设），非重入抽干 High；唯一真实影响 = 可重入代币下的 griefing/DoS（需攻击者控资产，属信任假设）。全工程 **7/7 PASS**（C1 4 + C2 3）。**铁律⑩：练手靶，不提交**。
- 交付物：`poc-c1-donation-inflation/{src/ReentrantERC20Lite.sol, test/PocC2.t.sol}`。

### 🔴 今日主线 3：Immunefi 一手侦察 + 321 github-fresh leads 筛选（用户指令）
- **Immunefi 一手侦察（curl explore HTML + 解析内嵌 JSON，非 WebFetch 猜测）**：`immunefi.com/explore/` 静态 HTML 内嵌 `bounties` 数组（转义 JSON，需 `\"`→`"` 反转义 + 字符串感知括号匹配抽数组）。解析出 **173 program**：kyc=False 71 / isPayToSubmit=False 134 / inviteOnly 全 False / EVM 152。
- **筛选条件**：`kyc=False ∧ isPayToSubmit=False ∧ EVM ∧ 非死路 ∧ 非已审无果 ∧ 非已提交被拒` → 得 **43 个可开审真靶**。
- **新鲜低竞争真靶（优先）**：enzyme-onyx(2025-09, AssetMgmt, $200k) / usdn(2025-04, Stablecoin, $50k, 链待核) / aster(2025-04, $200k, 链待核) / ens(2024-05, $250k) / orderlynetwork(2024-02, Derivatives, $100k) / mETH(2023-11, $500k, 链待核) / extrafinance(2023-09, Lending/Optimism, $100k) / utix(2023-07, $500k)。余 35 为 2020-2022 蓝筹红海（Synthetix/Lido/Balancer/Sushi/Yearn/PancakeSwap/Olympus/DeBridge 等）。
- **排除项**：已审无果 = velvetcapital / gammaswap；已提交被拒 = twyne(9/21 spam)；死路 = nexusmutual / fluxfinance / mtpelerin / ethena / thegraph / ssvnetwork / lombard / usdt0 / sky 等；非 EVM 误中 = hermetica / arkadiko（实为 Stacks）。
- **321 github-fresh 练手短名单（铁律⑩ 只练不提交，已排除 p1-p5 + defer 的 bazaar/awakening-core）**：优先 shoalfi/monorepo(217, 预言机+借贷新颖) / Cotejo(269, 隔离借贷+EIP-712) / The-Helix-Protocol(291, 交叉保证金+荷兰拍清算) / multyr(290+157, ERC-4626 Diamond-lite) / acex(274, 借贷+AMM) / 0g-restaking(270) / prism(123, 指数篮子 NAV) / VaultKeeper(85, ERC-4626+Automation)；次优 = Uniswap v4 hook 集群(jitter / afx-hook / tempest / hookarc / airbag-hook / volfee-hook / vixdex / orbithook)。
- **复用资产**：`bounty-recon/_parse_immunefi.py` + `_pivot.py` = Immunefi explore HTML 解析 + 分层筛选脚本；交付 `bounty-recon/pivot_shortlists.md`。

### 🔴 今日主线 4：重跑 Immunefi 侦察 · core-scope「在保面宽窄」专项筛选
- **重跑法**：curl `immunefi.com/explore/` 得 308 → `/bug-bounty/` 重定向、401KB 落盘（已修正：须 `-L` 跟重定向 + www 前缀 + 去 `--compressed` 避 schannel 握手失败）；`_pivot.py` 重解析得 **43 免押 EVM 候选**（同前序，无新靶，免疫台稳定）。
- **新增维度 = 在保面宽窄**（铁律② 无锚点不审的延伸：不仅要在保，还要在保**全核心 value-moving 代码**，否则挖到也交不了）：逐个拉 scope 页判 BROAD/NARROW。
- **BROAD（核心在保、可交）**：usdn($50k, 2025-04, ETH, 9 部署地址) / orderlynetwork($100k, 2024-02, 整 src+Vault) / staderforeth($1M, 2023-07, 13 地址) / extrafinance($100k, 2023-09, Op, 9 地址) / ipor($1k, BROAD 但赏池过低仅练手)。
- **NARROW/排除**：enzyme-onyx(仅 12 文件胶水) / aster(仅前端) / ens(web-heavy) / utix(1 合约+唯一 impact=解锁卡死) / magpie(1 合约)。
- **VERIFY**：mETH（`/meth/` 与 `/meth-protocol/` scope 均 404，疑下架/改名）。
- **铁律⑯ 收口**：NOW = **usdn** —— 43 免押中唯一同时满足「最新鲜(521d) + 在保覆盖全部核心 value-moving + 免押免 KYC + delta-neutral 稳定币复杂面（铸造/赎回/清算/预言机中间件，oracle 操纵/闪电贷未排除）」。orderly/stader/extrafinance 偏老红海 defer；ipor $1k 仅练手；31 蓝筹红海不入主线；mETH 待 Edge 活体核。
- **交付**：`bounty-recon/pivot_core_scope_shortlist.md`（分层表 + NOW/DEFER + usdn 开审前置）；已固化分层进 MEMORY.md 新节。

### 🔴 今日主线 5：usdn（SmarDex USDN，Immunefi 免押 $50k）四透镜深审 → **CLEAN 收口**
- 9 个部署地址全量四透镜深审（铸造/赎回/清算/预言机中间件面），**无非特权可触发、确定性、资金级 finding**。
- **铁律⑯ 收口**：usdn 四透镜 **CLEAN → 不提交**（宁漏报不误报，铁律⑭）；已归档 MEMORY.md 真靶实审段。

### 🔴 今日主线 6：staderforeth（Immunefi 免押 $1M，ETHx 流动性质押）四透镜深审 → **CLEAN 收口**
- **来源**：usdn CLEAN 收口后，主人拍板 (B) retire usdn + pivot 更大赏池 BROAD 靶 → 选 `staderforeth`($1M, 13 地址全在保) / `orderlynetwork`($100k) 二选一，先开 staderforeth。
- **仓库**：`git clone --depth 1 --branch mainnet_V0 https://github.com/stader-labs/ethx.git` 成功（53 .sol，lib/forge-std submodule；本次静态读码收口未编译）。
- **scope 锚点（一手核实）**：Max $1M / PoC 强制(Standard Badge) / **免 KYC** / Primacy of Impact 覆盖 C+H / OOS 含 Centralization risks / Prohibited 禁真实预言机 → PoC 须 local-fork + mock。12 显式在保地址**不含** SSPM/UWM/StaderOracle/Vault 等最大资金面 → 已标注铁律⑫ 活体核边界。
- **四透镜核心非特权面全 CLEAN（逐字回码 file:line）**：
  - ① 跨合约：存款 `deposit:188`→`_deposit:331`→`ETHx.mint:47`；取款 `finalizeUserWithdrawalRequest:135`→`burnFrom:56`；`receive/fallback` 均 revert（`PermissionedPool:52/57`）；value-moving 全 `onlyStaderContract`/`onlyRole` 守卫。
  - ② 经济/MEV/舍入：汇率由特权委员会驱动 + `updateWithInLimitER:737` BPS 上限，**非特权不可操纵 → MEV 不可达**；`previewDeposit:159` 向下取整保守；`finalize:154/163` 取小 + 旧值过减不超发。
  - ③ 权限：`deposit` 唯一公开资金入口仅铸等比 ETHx + min/max 闸门；`depositETHOverTargetWeight:234` 有 3 天 cooldown + 仅搬 Stader 自有 excess；`transferExcessETHToSSPM:170` 仅当 `preDepositValidatorCount==0` 转 Stader 自有金。
  - ④ 生命周期/重放/DoS：`claim:183` 先 `deleteRequestId:196` 后 `sendValue:197` 重入安全；`finalize:145/156` 批量有边界；safeMode/健康闸保守冻结。
- **残面 DEFER**：StaderOracle 委员会操纵(OOS 中央化)、SDUtilityPool/SDCollateral 高杠杆(OOS)、ValidatorWithdrawalVault(未深读)、finalize 极端 batch(低优先)、SD Auction MEV(Known Issue 有意设计)。
- **红海信号**：Halborn v2 + Sigma Prime v2 双顶级审计、Standard Badge、4 Known Issues、$1M 老牌 LST。
- **铁律⑯ 收口**：staderforeth 非特权核心面 **CLEAN + 红海强 + EV 偏低 → 同 usdn / 酶-onyx 收口不提交**。
- **交付**：`bounty-recon/staderforeth/AUDIT_NOTES.md`（四透镜 CLEAN + 全部锚点 + scope 疑点 + 残面 DEFER + 收口决策）。

### 📌 铁律⑯ 本轮 NOW 收口
- **现在最该做且只做一件**：**静默监控金标**（空窗未破 + 无合规新靶，强行深审 = 浪费；HOURLY 金标侦察自动化已在跑，新靶出现即触发 + Discord 告警）。
- **显式 defer**：① awakening-core AttestedPAT Medium（github-fresh 未挂平台，变现仅剩负责任披露，须主人拍板）；② 其余练手靶（lending C2 / bazaar / p1-p5）全 CLEAN 归档；③ usdn / staderforeth 已 CLEAN 归档；④ orderlynetwork($100k, perp-clearing 契合) / extrafinance($100k, lending-oracle 契合) 偏老红海 defer。
- **系统安全核验**：全程纯本地白帽分析 + 只读 WebSearch + 本地 scanner 拉取，**零登录 / 零提交 / 零私钥接触**。

### 📌 承接与待办
- **金标空窗连续自 8/31（今日第 34 天）**：铁律⑪ 维持静默监控；新 automation `1790201448486` HOURLY 跑 Immunefi 单源 + Discord 告警。
- **真靶**：TermMax V2 仍唯一 LIVE/ready（等 soldeer + mainnet-fork PoC + 用户 Edge 提交）；Twyne 转永久 dead；Midas 结案 / Tenor #407 parked（锁死真因已查实）/ Hinkal 不提交；orderly·gammaswap 四透镜 CLEAN。
- **非 EVM 路径**：用户 06:11 定「本机学」；按 `D:\non-evm-onboarding\README.md` 三选一装工具链（装 D 盘、先落 C 盘再移）。
- **环境限制记录**：沙箱内跑完整 gold_pipeline 会被进程组 kill（分源跑规避）；WSL 被安全策略禁用（须用户在安全中心开启）。
